{
  "id": "mod-exclusive-gateway-default-flow",
  "topic": "modeling",
  "subtopic": "gateways",
  "difficulty": "easy",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Echo Insurance routes 22000 claims per day in Camunda 8.8 through an exclusive gateway with three outgoing flows that compare a claim amount in euros. The architect wants the process to never raise an incident when no condition evaluates to true and instead take a fallback path.",
  "question": "How does the modeler guarantee that a fallback outgoing flow is taken when no condition expression evaluates to true?",
  "options": [
    { "id": "a", "text": "Mark one outgoing flow as the default flow on the gateway; it is taken when no other condition evaluates to true at runtime" },
    { "id": "b", "text": "Add an empty condition expression to one of the outgoing flows; the engine treats an empty expression as the default flow at runtime" },
    { "id": "c", "text": "Enable the exclusiveGateway has-default attribute on the diagram; the first outgoing flow in document order is taken as the default" },
    { "id": "d", "text": "Add a boundary error event with code none to the gateway; the engine routes the token through the boundary path as the fallback" }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": { "text": "Correct. The Camunda 8 docs describe the default flow attribute on an exclusive gateway as the fallback taken when no other condition is true; no incident is raised." },
    "b": { "text": "Incorrect. Empty condition expressions are invalid on conditional flows; the default flow is set explicitly through the gateway's default flow property." },
    "c": { "text": "Incorrect. There is no such attribute; the default flow is a property of one specific outgoing flow, not of the gateway in document order." },
    "d": { "text": "Incorrect. Boundary error events apply to activities and require a thrown error; they are not a fallback mechanism for an exclusive gateway." }
  },
  "explanation": "Exclusive gateways support a designated default flow taken when no condition is true, which prevents incidents and routes the token through a fallback path.",
  "docs": [
    { "title": "BPMN exclusive gateway", "url": "https://docs.camunda.io/docs/components/modeler/bpmn/exclusive-gateways/" }
  ]
}
