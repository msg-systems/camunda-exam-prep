{
  "id": "mod-inclusive-gateway",
  "topic": "modeling",
  "subtopic": "gateways",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Granite Bank evaluates 12000 loans per day in Camunda 8.8. After a gateway, a token must follow every outgoing flow whose condition is true (one, two, or all three branches may apply) and later synchronize on a single matching join gateway downstream.",
  "question": "Which gateway type matches a token to every outgoing flow whose condition is true, and synchronizes only those branches at the join?",
  "options": [
    { "id": "a", "text": "Inclusive gateway forwards a token on every outgoing flow whose condition is true and the matching join waits for exactly those branches" },
    { "id": "b", "text": "Parallel gateway forwards a token on every outgoing flow unconditionally and the join waits for every incoming branch, conditional or not" },
    { "id": "c", "text": "Exclusive gateway forwards a token to a single outgoing flow that matches the first true condition in document order at evaluation" },
    { "id": "d", "text": "Event-based gateway forwards a token to the outgoing flow whose intermediate catch event triggers first and ignores all others" }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": { "text": "Correct. The Camunda 8 docs describe the inclusive gateway as taking every flow with a true condition at the split and synchronizing exactly those branches at the join." },
    "b": { "text": "Incorrect. The parallel gateway is unconditional; it does not evaluate conditions on outgoing flows at the split." },
    "c": { "text": "Incorrect. The exclusive gateway forwards to exactly one outgoing flow, not to several conditionally." },
    "d": { "text": "Incorrect. Event-based gateways race catching events; they do not evaluate FEEL conditions on outgoing flows at all." }
  },
  "explanation": "Inclusive gateways follow every outgoing flow whose condition is true and synchronize matching branches at the join, supporting one-of-many or several-of-many semantics.",
  "docs": [
    { "title": "BPMN inclusive gateway", "url": "https://docs.camunda.io/docs/components/modeler/bpmn/inclusive-gateways/" }
  ]
}
