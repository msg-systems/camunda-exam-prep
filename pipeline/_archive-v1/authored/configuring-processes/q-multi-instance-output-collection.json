{
  "id": "cp-multi-instance-output-collection",
  "topic": "configuring-processes",
  "subtopic": "multi-instance",
  "difficulty": "hard",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Zenith Insurance processes 6000 claims per day in Camunda 8.8. A parallel multi-instance service task computes a per-iteration variable priceEur; on completion the team must collect every iteration's priceEur into a single list called prices on the parent scope for downstream FEEL aggregation.",
  "question": "Which two properties configure the per-iteration output collection on the multi-instance task?",
  "options": [
    { "id": "a", "text": "Set outputCollection to prices and outputElement to priceEur on the multi-instance marker so each iteration contributes its priceEur to the list" },
    { "id": "b", "text": "Set inputCollection to prices and inputElement to priceEur on the multi-instance marker so each iteration contributes its priceEur to the list" },
    { "id": "c", "text": "Set outputCollection to priceEur and outputElement to prices on the multi-instance marker so the engine reverses the mapping at completion" },
    { "id": "d", "text": "Set loop cardinality to a FEEL expression like count(prices) and let the engine accumulate priceEur into prices automatically at completion" }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": { "text": "Correct. outputCollection names the target list on the parent scope, and outputElement names the per-iteration variable that contributes to the list." },
    "b": { "text": "Incorrect. inputCollection and inputElement drive the iteration input, not the collected output list on the parent scope." },
    "c": { "text": "Incorrect. The roles are not reversible: outputCollection is the list name, outputElement is the per-iteration value name." },
    "d": { "text": "Incorrect. Loop cardinality is just a count; it does not collect per-iteration variables into an output list." }
  },
  "explanation": "outputCollection names the list on the parent scope and outputElement names the per-iteration variable that contributes to that list at completion.",
  "docs": [
    { "title": "BPMN multi-instance", "url": "https://docs.camunda.io/docs/components/modeler/bpmn/multi-instance/" }
  ]
}
