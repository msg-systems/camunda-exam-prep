{
  "id": "cp-multi-instance-input-collection",
  "topic": "configuring-processes",
  "subtopic": "multi-instance",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "North Marketplace processes 14000 orders per day in Camunda 8.8. A multi-instance service task must run once per element of a list called lineItems on the process scope. The team must configure exactly which collection iterations consume and which variable each iteration sees.",
  "question": "Which two configuration fields on the multi-instance task are correct?",
  "options": [
    { "id": "a", "text": "Set inputCollection to a FEEL expression evaluating to the list, and set inputElement to the variable name each iteration should see" },
    { "id": "b", "text": "Set outputCollection to a FEEL expression evaluating to the list, and set outputElement to the variable name each iteration should see" },
    { "id": "c", "text": "Set the loop cardinality to a FEEL expression returning the list, and set inputElement to the variable name each iteration should see" },
    { "id": "d", "text": "Set the inputCollection on the parent process scope, not on the activity, and use a per-element FEEL header to expose the element to each iteration" }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": { "text": "Correct. Camunda 8 multi-instance markers use inputCollection for the iteration list and inputElement for the per-iteration variable name." },
    "b": { "text": "Incorrect. outputCollection accumulates per-iteration outputs into a list; it does not drive the iteration count." },
    "c": { "text": "Incorrect. Loop cardinality is an integer count; inputCollection is the list-driven mechanism that pairs with inputElement." },
    "d": { "text": "Incorrect. The multi-instance configuration lives on the activity element itself, not on the parent process scope via a header." }
  },
  "explanation": "Camunda 8 multi-instance markers use inputCollection (the list) and inputElement (the per-iteration variable name) to drive list-based iteration.",
  "docs": [
    { "title": "BPMN multi-instance", "url": "https://docs.camunda.io/docs/components/modeler/bpmn/multi-instance/" }
  ]
}
