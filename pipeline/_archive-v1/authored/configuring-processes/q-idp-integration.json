{
  "id": "cp-idp-integration",
  "topic": "configuring-processes",
  "subtopic": "IDP",
  "difficulty": "hard",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Wren Bank processes 8000 invoices per day in Camunda 8.8 with Intelligent Document Processing. The team must extract a configured set of fields (invoiceNumber, totalEur, dueDate) from each uploaded PDF document inside the BPMN flow and write the extracted values back to process variables for downstream business rule evaluation.",
  "question": "Which Camunda 8.8 BPMN element invokes the IDP extraction step and persists the extracted fields as process variables?",
  "options": [
    { "id": "a", "text": "An IDP application invoked from a service task or connector; the configured field set is extracted and written back as named process variables" },
    { "id": "b", "text": "A user task with IDP enabled on the form; Tasklist runs the extraction client-side and writes the resulting values to process variables" },
    { "id": "c", "text": "A receive task subscribed to a message named idp-extract; the broker correlates extraction results from the document store automatically" },
    { "id": "d", "text": "A business rule task with IDP as the decision engine type; the engine runs OCR plus DMN in one call against the uploaded document" }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": { "text": "Correct. IDP is invoked from a service task or a dedicated connector; the documented field set is extracted and written back as process variables." },
    "b": { "text": "Incorrect. Tasklist does not run IDP client-side; extraction is a backend service or connector invocation in Camunda 8.8." },
    "c": { "text": "Incorrect. There is no implicit idp-extract message subscription; IDP is invoked synchronously from a task element." },
    "d": { "text": "Incorrect. Business rule tasks evaluate DMN; IDP is a separate component invoked from a service task or connector, not a DMN engine type." }
  },
  "explanation": "Camunda 8.8 IDP runs as an application invoked from a service task or connector; the extracted field set is written back as process variables for downstream steps.",
  "docs": [
    { "title": "Intelligent Document Processing", "url": "https://docs.camunda.io/docs/components/idp/" }
  ]
}
