{
  "id": "cp-user-task-candidate-groups",
  "topic": "configuring-processes",
  "subtopic": "user task",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Quantum Marketplace runs 2000 events per day in Camunda 8.8 across 3 candidate groups in Tasklist. A user task must be visible to a group selected dynamically per process instance based on the variable productLine. The team must configure the user task accordingly.",
  "question": "Which user task property selects the candidate group from a process variable at activation?",
  "options": [
    { "id": "a", "text": "Set the candidate groups property to a FEEL expression like =productLine so the engine resolves the group at activation and exposes the task" },
    { "id": "b", "text": "Set the candidate groups property to the literal productLine so the engine treats the value as a variable reference for backward compatibility" },
    { "id": "c", "text": "Set the assignee property to a FEEL expression like =productLine and let Tasklist convert the user name to a group automatically at sign-in" },
    { "id": "d", "text": "Set the candidate users property to a FEEL expression like =productLine and let Tasklist expand the value into the matching candidate group" }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": { "text": "Correct. Camunda 8 user tasks accept a FEEL candidate groups expression evaluated at activation; the engine writes the resolved group id to the task." },
    "b": { "text": "Incorrect. The literal value is a group name string, not an implicit variable reference; explicit FEEL expressions are required." },
    "c": { "text": "Incorrect. Assignee writes the owner; it does not convert a user name into a candidate group implicitly in Tasklist." },
    "d": { "text": "Incorrect. Candidate users define a per-user allow list; they do not expand into group ids in Camunda 8 Tasklist." }
  },
  "explanation": "Candidate groups on a user task accept a FEEL expression in Camunda 8; the engine evaluates it at activation to determine which Tasklist groups see the task.",
  "docs": [
    { "title": "BPMN user task", "url": "https://docs.camunda.io/docs/components/modeler/bpmn/user-tasks/" }
  ]
}
