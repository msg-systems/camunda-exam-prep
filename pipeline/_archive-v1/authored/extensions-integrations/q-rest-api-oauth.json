{
  "id": "ext-rest-api-oauth",
  "topic": "extensions-integrations",
  "subtopic": "REST API",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Junction Telco's batch job calls the Camunda 8.8 REST API for 4000 transactions per day on Camunda SaaS. The team must authenticate the service-to-service calls without an interactive user, using a long-lived machine identity issued by the platform's identity provider.",
  "question": "Which OAuth grant authenticates the Camunda 8 REST API for machine-to-machine calls?",
  "options": [
    { "id": "a", "text": "The client credentials grant; the service exchanges a client id and client secret for an access token from the Identity token endpoint" },
    { "id": "b", "text": "The authorization code grant; the service redirects to the Identity authorization endpoint and exchanges a one-time code for a token on each call" },
    { "id": "c", "text": "The resource owner password grant; the service stores a username and password and exchanges them for a token on every batch call to the API" },
    { "id": "d", "text": "The implicit grant; the service sends a request to the Identity authorize endpoint and receives a token in the URL fragment on the redirect" }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": { "text": "Correct. Camunda 8 machine-to-machine API calls use the OAuth client credentials grant; the service exchanges client id and secret for an access token." },
    "b": { "text": "Incorrect. Authorization code is for interactive users with browsers, not for service-to-service calls." },
    "c": { "text": "Incorrect. The password grant is deprecated and not the documented mechanism for Camunda 8 machine calls." },
    "d": { "text": "Incorrect. The implicit grant is deprecated and not the documented mechanism for Camunda 8 machine calls." }
  },
  "explanation": "Camunda 8 machine-to-machine API access uses the OAuth client credentials grant; clients are issued a client id and secret to obtain tokens.",
  "docs": [
    { "title": "Authentication", "url": "https://docs.camunda.io/docs/apis-tools/orchestration-cluster-api-rest/authentication/" }
  ]
}
