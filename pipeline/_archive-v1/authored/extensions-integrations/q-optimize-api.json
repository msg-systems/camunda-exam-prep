{
  "id": "ext-optimize-api",
  "topic": "extensions-integrations",
  "subtopic": "Optimize API",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Xander Bank exports historical process analytics for 22000 transactions per day in Camunda 8.8 to a downstream BI platform. The team must fetch the JSON definition of a saved Optimize report so the BI platform can render the same chart configuration without rebuilding the query externally.",
  "question": "Which Camunda 8 Optimize API capability exports a saved report definition for external use?",
  "options": [
    { "id": "a", "text": "Optimize exposes a Public API to read a saved report by id; the response contains the report definition for external rendering or programmatic import" },
    { "id": "b", "text": "Optimize exposes a Zeebe stream of report results; clients subscribe to the stream and receive the saved report definition once per export interval over gRPC" },
    { "id": "c", "text": "Optimize embeds a Kibana endpoint; clients query the embedded path to retrieve the report definition as a Kibana saved object on each export" },
    { "id": "d", "text": "Optimize exposes a CSV-only export; clients can read the rendered report data but the original definition is only available through the UI export dialog" }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": { "text": "Correct. Camunda 8 Optimize exposes a Public API that retrieves saved report definitions by id; the response is JSON for external consumption." },
    "b": { "text": "Incorrect. Optimize results are not streamed over Zeebe gRPC; the Public API is the documented external surface." },
    "c": { "text": "Incorrect. There is no embedded Kibana endpoint exposed by Optimize; the Public API is the documented surface." },
    "d": { "text": "Incorrect. The Public API also exposes the report definition as JSON, not only the rendered CSV data." }
  },
  "explanation": "Optimize's Public API retrieves saved report definitions and data; clients integrate by calling the API with OAuth-issued tokens.",
  "docs": [
    { "title": "Optimize Public API", "url": "https://docs.camunda.io/docs/apis-tools/optimize-api/optimize-api-authorization/" }
  ]
}
