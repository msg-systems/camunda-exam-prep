{
  "id": "mgmt-operate-batch-operations",
  "topic": "managing-development",
  "subtopic": "Operate",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Echo Bank cleans up after an outage that left 2000 incidents on the same activity in Camunda 8.8. The operator must apply one operation across every matching process instance from a filtered list view in Operate without clicking through each instance one at a time.",
  "question": "Which Operate feature applies cancel, retry, or delete operations to every result of a filtered list of process instances at once?",
  "options": [
    { "id": "a", "text": "Batch operations in Operate apply cancel, retry, or delete to every instance in the current filtered list view in a single operator action" },
    { "id": "b", "text": "Batch operations in Operate apply cancel, retry, or delete only after the operator exports the filtered list and re-imports a CSV of instance keys" },
    { "id": "c", "text": "Batch operations in Operate are limited to retrying incidents and require a separate API call to cancel or delete process instances in bulk" },
    { "id": "d", "text": "Batch operations in Operate apply cancel and delete only to instances already in the completed state, never to running instances with incidents" }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": { "text": "Correct. Operate's batch operations cover retry, cancel, and delete on the current filtered list view in a single operator action." },
    "b": { "text": "Incorrect. There is no required CSV re-import step; batch operations act on the filtered list directly in the UI." },
    "c": { "text": "Incorrect. Cancel and delete are supported as batch operations alongside retry, all in the same Operate UI." },
    "d": { "text": "Incorrect. Batch cancel and delete apply to running instances and matching incidents, not only to completed ones." }
  },
  "explanation": "Operate exposes batch operations (retry, cancel, delete) that apply to every instance of a filtered list view in a single operator action.",
  "docs": [
    { "title": "Operate batch operations", "url": "https://docs.camunda.io/docs/components/operate/userguide/operations-on-multiple-instances/" }
  ]
}
