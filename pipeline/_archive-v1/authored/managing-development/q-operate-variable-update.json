{
  "id": "mgmt-operate-variable-update",
  "topic": "managing-development",
  "subtopic": "Operate",
  "difficulty": "easy",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Catalyst Logistics ships 6000 shipments per day in Camunda 8.8. A clerk entered the wrong customerEmail variable on a running process instance. The operator must update that variable on the running instance through Operate without restarting the instance from its start event.",
  "question": "Which Operate action updates a single process variable on a running instance without restarting the instance?",
  "options": [
    { "id": "a", "text": "Edit the variable value in the variables panel of the instance detail page; the new value is persisted on the instance scope immediately" },
    { "id": "b", "text": "Cancel the running instance and start a new instance with the corrected variable using the same business key on the process definition page" },
    { "id": "c", "text": "Add a new boundary error event to the running activity through Operate to capture the stale value and replace it on the boundary path" },
    { "id": "d", "text": "Publish a correction message to the broker through Operate; the engine then overwrites the variable on the matching subscription" }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": { "text": "Correct. Operate allows operators to edit variable values directly on the running instance through the variables panel of the instance detail page." },
    "b": { "text": "Incorrect. Cancelling and restarting loses progress and reorders work; Operate supports in-place variable updates on the running instance." },
    "c": { "text": "Incorrect. Operate does not allow operators to add BPMN elements to a running instance; the runtime BPMN model is immutable." },
    "d": { "text": "Incorrect. Operate's variable update is a direct edit; there is no special correction-message correlation mechanism for variable overwrites." }
  },
  "explanation": "Operate exposes a variables panel on the instance detail page that lets operators update variables on the running instance scope without restarting the instance.",
  "docs": [
    { "title": "Operate variables", "url": "https://docs.camunda.io/docs/components/operate/userguide/resolve-incidents-update-variables/" }
  ]
}
