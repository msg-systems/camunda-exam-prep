{
  "id": "mgmt-identity-client-credentials",
  "topic": "managing-development",
  "subtopic": "Identity",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Granite Telco runs a backend service against Camunda 8.8 SaaS that calls the Zeebe gRPC API for 18000 jobs per hour. The service must authenticate without any human user being involved at request time; credentials are issued once and refreshed automatically by the SDK in the running deployment.",
  "question": "Which OAuth grant type fits this machine-to-machine authentication flow with Camunda Identity?",
  "options": [
    { "id": "a", "text": "Client credentials grant; the service exchanges its client id and client secret for a bearer token at the Identity token endpoint" },
    { "id": "b", "text": "Authorization code grant; the service exchanges a redirect-issued code for a bearer token at the Identity token endpoint for each call" },
    { "id": "c", "text": "Resource owner password grant; the service stores a service-account username and password and exchanges them for a token on each call" },
    { "id": "d", "text": "Implicit grant; the service obtains a bearer token directly from the authorization endpoint without going through the token endpoint at all" }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": { "text": "Correct. The Camunda 8 Identity docs prescribe the client credentials grant for machine-to-machine flows with no human user, using a client id and secret." },
    "b": { "text": "Incorrect. Authorization code grant requires a browser redirect and a human-resource owner; it does not fit a backend service flow." },
    "c": { "text": "Incorrect. Resource owner password grant is discouraged and not the documented flow for backend services in Camunda 8 Identity." },
    "d": { "text": "Incorrect. Implicit grant is for single-page applications and is also legacy; client credentials is the documented machine-to-machine grant." }
  },
  "explanation": "Client credentials grant is the documented OAuth 2.0 flow in Camunda 8 Identity for backend services calling the Zeebe gRPC and REST APIs without human users.",
  "docs": [
    { "title": "Camunda 8 SaaS authentication", "url": "https://docs.camunda.io/docs/components/console/manage-clusters/manage-api-clients/" }
  ]
}
