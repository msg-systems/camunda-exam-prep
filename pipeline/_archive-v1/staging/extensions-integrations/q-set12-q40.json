{
  "id": "imp-set12-q40",
  "topic": "extensions-integrations",
  "subtopic": "imported",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "A FEEL expression must compute the Cartesian product of two lists: for every pair (customer, product), generate a record. They wonder if FEEL's for expression supports multiple iterators. Does FEEL for expression support multiple iterators?",
  "question": "Does FEEL for expression support multiple iterators?",
  "options": [
    {
      "id": "a",
      "text": "FEEL allows multiple comma-separated iterators in a single for expression, producing a Cartesian product of all the input collections. count entries."
    },
    {
      "id": "b",
      "text": "Using nested for expressions to compute a Cartesian product is technically workable but produces a different result shape than the multi-iterator form. A nested for like for c in customers return for p in products return {customer: c, product: p} produces a list of lists -- one inner list per customer -- rather than a single flat list of all pairs. To achieve the same flat result as the multi-iterator form, the nested result would need to be flattened. The multi-iterator for c in customers, p in products return ..."
    },
    {
      "id": "c",
      "text": "FEEL's for expression supports multiple comma-separated iterators in a single expression, which produces a Cartesian product of the input collections. count entries."
    },
    {
      "id": "d",
      "text": "FEEL includes a for expression as a core part of its language specification. The syntax for variable in collection return expression iterates over a collection and transforms each element, producing a new list. FEEL's for expression supports multiple iterators, range-based iteration using the .. 10), and filter conditions using where. This construct is one of the primary ways to process lists and generate derived collections in FEEL, and it is fully supported in Camunda 8's FEEL engine implementation."
    }
  ],
  "correctOptionId": "c",
  "optionExplanations": {
    "a": {
      "text": "Incorrect. FEEL's for expression syntax: ` for [, ] return Each iterator is variableName in collection. Multiple iterators produce Cartesian product: =for c in [1, 2], d in [10, 20] return c d Result: [10, 20, 20, 40] -- products of all (c, d) pairs. Practical use: Generate all combinations: =for c in customers, p in products return {customer: c, product: p} -- every customer × every product. Range-based: =for i in 1..10, j in 1..10 return {i: i, j: j} -- 10×10 = 100 records. With filter: =for c in customers, p in products where matches(c, p) return ... -- combined with filter conditions. Comparison with nested for: =for c in customers return for p in products return {customer: c, product: p} This produces a list of lists (one inner list per customer). Different result shape: flatten for the same Cartesian result. Multi-iterator for is flatter and more idiomatic. Performance: Cartesian product can be large; 1000 × 1000 = 1,000,000` records. Be mindful of resulting list size."
    },
    "b": {
      "text": "Incorrect. Multiple supported."
    },
    "c": {
      "text": "Correct. Option d) -- Wrong. Core feature."
    },
    "d": {
      "text": "Incorrect. wrong."
    }
  },
  "explanation": "Option d) -- Wrong. Core feature.",
  "docs": [
    {
      "title": "Camunda 8 docs",
      "url": "https://docs.camunda.io/docs/components/modeler/feel/"
    }
  ],
  "sourceSet": 12,
  "sourceId": "set12-q40",
  "_lint": {
    "clean": true,
    "findings": []
  }
}
