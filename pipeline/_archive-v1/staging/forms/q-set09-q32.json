{
  "id": "imp-set09-q32",
  "topic": "forms",
  "subtopic": "imported",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Tasklist users access the app from desktop and mobile. The team's Forms have 12 fields. They want the form to render well on mobile -- responsive layout, touch-friendly inputs.",
  "question": "How does Camunda Forms handle responsive design for mobile?",
  "options": [
    {
      "id": "a",
      "text": "Camunda Forms are designed to render correctly on mobile devices as a core feature of the form-js rendering library. The renderer uses responsive CSS techniques that adapt the layout to various viewport widths. Claiming that Forms lack mobile support contradicts both the design intent and the practical deployment experience of Tasklist users who access tasks on mobile browsers. Mobile-friendly behaviour is a baseline characteristic of the platform."
    },
    {
      "id": "b",
      "text": "There is no per-field responsive: true property in Camunda Forms configuration. Responsive behaviour is handled at the renderer level, not configured field by field in the form schema. The form-js renderer applies responsive CSS rules globally based on the current viewport width, automatically stacking or arranging fields appropriately without requiring any per-element responsive configuration by the modeller. Teams embedding form-js in custom applications can override CSS for further layout control."
    },
    {
      "id": "c",
      "text": "Camunda Forms render in responsive layouts by default -- the form's renderer (in Tasklist or custom apps embedding the form-js library) adapts to viewport width. Fields stack vertically on narrow screens, side-by-side on wider screens. Modelers don't typically need to configure responsive behaviour explicitly; the renderer handles it. For custom UI / embedding, theming hooks let teams customise."
    },
    {
      "id": "d",
      "text": "It doubles the maintenance burden because any change to the form logic or fields must be applied twice, introducing synchronisation errors over time. A single Camunda Form definition renders responsively across all screen sizes using the form-js renderer's built-in responsive layout capabilities, eliminating the need for separate mobile variants."
    }
  ],
  "correctOptionId": "c",
  "optionExplanations": {
    "a": {
      "text": "Incorrect. Camunda Forms are built with responsive design as a baseline. The form-js renderer (used by Tasklist and embeddable apps) uses responsive CSS techniques (typically flexbox / grid + media queries) to adapt to viewport width. Common adaptations: Narrow viewports (mobile): fields stack vertically, full-width. Medium viewports (tablet): fields may wrap intelligently. Wide viewports (desktop): fields can be side-by-side. For specific layout fine-tuning, some Forms element libraries support grouping / columns that scale with viewport. For deep customisation (e.g., embedded in a corporate-branded portal), the form-js library can be themed / styled with custom CSS."
    },
    "b": {
      "text": "Incorrect. Mobile-friendly."
    },
    "c": {
      "text": "Correct. Renderer-level, not per-field."
    },
    "d": {
      "text": "Incorrect. Single responsive form."
    }
  },
  "explanation": "Renderer-level, not per-field.",
  "docs": [
    {
      "title": "Camunda 8 docs",
      "url": "https://docs.camunda.io/docs/components/modeler/forms/"
    }
  ],
  "sourceSet": 9,
  "sourceId": "set09-q32",
  "_lint": {
    "clean": true,
    "findings": []
  }
}
