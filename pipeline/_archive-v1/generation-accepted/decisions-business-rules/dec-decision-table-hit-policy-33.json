{
  "id": "dec-decision-table-hit-policy-33",
  "topic": "decisions-business-rules",
  "subtopic": "hit policy",
  "difficulty": "hard",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Nimbus retailer models a Camunda 8.8 DMN table that totals discount percentages across 18000 orders per day. The modeler picks COLLECT and configures aggregation=\"SUM\" on the decisionTable element to combine matched rules. The reviewer asks how many output columns the decision table may declare under that configuration.",
  "question": "How many output columns is the decision table allowed to declare when COLLECT is used with an aggregator on the decisionTable element?",
  "options": [
    {
      "id": "a",
      "text": "Up to five output columns are allowed, and the aggregator is applied independently to each output column during the evaluation of the decision table"
    },
    {
      "id": "b",
      "text": "The decision table can have only one output, and the aggregator produces a single output entry from the outputs of the satisfied rules during evaluation"
    },
    {
      "id": "c",
      "text": "An unlimited number of output columns is allowed, and the aggregator is applied independently to every output column during the evaluation of the decision table"
    },
    {
      "id": "d",
      "text": "Two output columns are allowed, with the aggregator applied to the first column and the second column reserved for an aggregated explanation entry during evaluation"
    }
  ],
  "correctOptionId": "b",
  "optionExplanations": {
    "a": {
      "text": "Incorrect. The docs do not allow five output columns under an aggregated COLLECT policy; the info callout explicitly restricts the decision table to a single output when an aggregator is used."
    },
    "b": {
      "text": "Correct. The COLLECT info callout states that if the COLLECT hit policy is used with an aggregator, the decision table can only have one output, which the aggregator combines into a single output entry."
    },
    "c": {
      "text": "Incorrect. The docs explicitly cap the decision table at one output when COLLECT is paired with an aggregator; an unlimited count contradicts the info callout in the COLLECT section."
    },
    "d": {
      "text": "Incorrect. The docs do not document a two-output layout for aggregated COLLECT; the limit is one output, and there is no documented explanation entry produced by the aggregator."
    }
  },
  "explanation": "The COLLECT section includes an info callout stating that if the COLLECT hit policy is used with an aggregator, the decision table can only have one output. The aggregator combines the outputs of all satisfied rules into that single output entry.",
  "docs": [
    {
      "title": "DMN hit policy",
      "url": "https://docs.camunda.io/docs/components/modeler/dmn/decision-table-hit-policy/"
    }
  ]
}
