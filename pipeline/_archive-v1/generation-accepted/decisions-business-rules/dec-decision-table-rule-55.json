{
  "id": "dec-decision-table-rule-55",
  "topic": "decisions-business-rules",
  "subtopic": "decision-table-rule",
  "difficulty": "easy",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Granite Bank uses a Camunda 8.8 DMN decision table to classify 600 transactions per hour into high or low risk buckets. A junior reviewer asks how many rules the table is required to contain in order to be a valid decision table according to the DMN docs.",
  "question": "How many rules can a single DMN decision table contain according to the documented model?",
  "options": [
    {
      "id": "a",
      "text": "A decision table can contain one or more rules, with each rule formed by its own input entries and output entries"
    },
    {
      "id": "b",
      "text": "A decision table must contain at least two rules so that the hit policy attribute has multiple candidates to combine"
    },
    {
      "id": "c",
      "text": "A decision table must contain exactly one rule whose multiple output entries return the full evaluation result"
    },
    {
      "id": "d",
      "text": "A decision table can contain zero or more rules, and an empty table returns its declared default output entries"
    }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": {
      "text": "Correct. The DMN docs state that a decision table can have one or more rules and that each rule is built from its own input entries (conditions) and output entries (conclusions)."
    },
    "b": {
      "text": "Incorrect. The DMN docs describe rule counts starting at one; a single rule is a valid decision table even though it leaves no room for hit-policy resolution between rules."
    },
    "c": {
      "text": "Incorrect. A decision table can have many rules in addition to one; the DMN docs explicitly say one or more rules, not exactly one rule per table."
    },
    "d": {
      "text": "Incorrect. The DMN docs require at least one rule on a decision table; the documented model does not describe a zero-rule table with default output entries."
    }
  },
  "explanation": "A DMN decision table can have one or more rules. Each rule is made up of input entries that hold the conditions and output entries that hold the conclusions, and the table is evaluated against the supplied input data by checking every rule's conditions.",
  "docs": [
    {
      "title": "DMN decision table rule",
      "url": "https://docs.camunda.io/docs/components/modeler/dmn/decision-table-rule/"
    }
  ]
}
