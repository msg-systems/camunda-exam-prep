{
  "id": "mng-process-instance-modification-29",
  "topic": "managing-development",
  "subtopic": "Operate",
  "difficulty": "easy",
  "style": "concept",
  "camundaVersion": "8.8",
  "scenario": "A logistics operator at Atlas Logistics runs Camunda 8.8 across 800 shipments per day and queues a cancel-all modification on a single flow node nested in a multi-instance body that currently holds 1200 running tokens for a shipment-fanout pattern. Before applying, the operator needs to confirm how many tokens a single such modification can affect in this version so the team can plan repetitions.",
  "question": "How many tokens can a single cancel-all or move-all modification affect on a flow node nested in a multi-instance body?",
  "options": [
    {
      "id": "a",
      "text": "Up to 100 tokens can be affected per modification, and the operator must repeat the cancel or move modification on the same flow node until all remaining running tokens have been handled"
    },
    {
      "id": "b",
      "text": "Up to 250 tokens can be affected per modification, and the operator must repeat the cancel or move modification on the same flow node until all remaining running tokens have been handled"
    },
    {
      "id": "c",
      "text": "Up to 1000 tokens can be affected per modification, and the operator must repeat the cancel or move modification on the same flow node until all remaining running tokens have been handled"
    },
    {
      "id": "d",
      "text": "Up to 500 tokens can be affected per modification, and the operator must repeat the cancel or move modification on the same flow node until all remaining running tokens have been handled"
    }
  ],
  "correctOptionId": "d",
  "optionExplanations": {
    "a": {
      "text": "Incorrect. The documented technical limit is 500 tokens per cancel-or-move modification on a flow node nested in a multi-instance body, not 100. Only 500 tokens are affected per repetition until all have been handled."
    },
    "b": {
      "text": "Incorrect. The documented technical limit is 500 tokens per cancel-or-move modification on a flow node nested in a multi-instance body, not 250. Only 500 tokens are affected per repetition until all have been handled."
    },
    "c": {
      "text": "Incorrect. The documented technical limit is 500 tokens per cancel-or-move modification on a flow node nested in a multi-instance body, not 1000. Only 500 tokens are affected per repetition until all have been handled."
    },
    "d": {
      "text": "Correct. The docs specify that when cancelling or moving all tokens within a single flow node instance nested in a multi-instance body, a maximum of 500 tokens can be affected per modification, and operators repeat the modification until all tokens are handled."
    }
  },
  "explanation": "Cancelling or moving all tokens within a single flow node nested in a multi-instance body is capped at 500 tokens per modification in this version. Operators repeat the modification on the same flow node, or use the Orchestration Cluster API with a custom filter for finer control.",
  "docs": [
    {
      "title": "Process instance modification",
      "url": "https://docs.camunda.io/docs/components/operate/userguide/process-instance-modification/"
    }
  ]
}
