{
  "id": "mng-process-instance-modification-40",
  "topic": "managing-development",
  "difficulty": "hard",
  "style": "scenario",
  "camundaVersion": "8.8",
  "docs": [
    {
      "title": "Process instance modification",
      "url": "https://docs.camunda.io/docs/components/operate/userguide/process-instance-modification/"
    }
  ],
  "subtopic": "Operate",
  "scenario": "Aurora Insurance is running Camunda 8.8 and an operator has 250 claim instances paused at a Review Claim service task that is no longer needed for one specific instance. The operator opens that single instance in Operate modification mode and wants to remove every running token currently sitting on Review Claim before applying the change.",
  "question": "Which sequence in modification mode cancels all running tokens on the Review Claim flow node for this instance?",
  "options": [
    {
      "id": "a",
      "text": "The operator selects the Review Claim flow node in the diagram and picks Cancel instance from its dropdown, and the cancellation appears as a pending modification in the instance history"
    },
    {
      "id": "b",
      "text": "The operator selects the Review Claim flow node and picks Skip token, after which Operate immediately removes the token from the instance without any pending modification entry being created"
    },
    {
      "id": "c",
      "text": "The operator opens the variables panel, clears the value that fed Review Claim, and the engine then automatically retires every running token sitting on that flow node for the instance"
    },
    {
      "id": "d",
      "text": "The operator highlights Review Claim and chooses Move instance, then leaves the target empty so that the running tokens are dropped rather than moved to another flow node in the diagram"
    }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": {
      "text": "Correct. The docs say that to cancel all running tokens on a flow node, the operator selects the flow node and clicks Cancel instance from the dropdown; the cancellation is then visible as a pending modification reflected in the instance history."
    },
    "b": {
      "text": "Incorrect. There is no Skip token action in the documented modification flow, and modifications are always staged as pending entries before they are applied, not removed immediately."
    },
    "c": {
      "text": "Incorrect. Cancelling tokens on a flow node is an explicit modification action in Operate; clearing a variable value does not automatically retire running tokens sitting on a service task."
    },
    "d": {
      "text": "Incorrect. The Move instance action requires choosing a target flow node to receive the tokens; leaving the target empty is not how tokens are cancelled in the documented flow."
    }
  },
  "explanation": "To cancel all running tokens on a single flow node, the operator selects the flow node in modification mode and clicks Cancel instance from its dropdown. The pending cancellation is then reflected in the instance history before being applied."
}
