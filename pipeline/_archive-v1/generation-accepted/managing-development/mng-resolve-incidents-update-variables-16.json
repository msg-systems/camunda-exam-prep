{
  "id": "mng-resolve-incidents-update-variables-16",
  "topic": "managing-development",
  "subtopic": "Operate",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Vertex Insurance processes 2400 claims per day on Camunda 8.8. An operator inspects a stuck instance in Operate where the orderValue variable holds the string \"4250\" instead of the integer 4250, and the operator needs to correct the variable in place from the instance variables panel without redeploying the process model.",
  "question": "Which sequence of clicks in Operate corrects the orderValue variable on the affected process instance?",
  "options": [
    {
      "id": "a",
      "text": "Click the variable name to rename it, type the integer 4250 into the rename dialog, and press Enter to commit the new value to the broker on the cluster"
    },
    {
      "id": "b",
      "text": "Click the edit icon next to orderValue, remove the quotation marks so the value becomes the integer 4250, then click the checkmark icon to save the change"
    },
    {
      "id": "c",
      "text": "Click the variable row to open a script console, paste a FEEL expression setting orderValue to 4250, then click run to apply the change to the broker"
    },
    {
      "id": "d",
      "text": "Click the incident banner to open Operate settings, change the type for orderValue from string to integer, then click apply to update the value here"
    }
  ],
  "correctOptionId": "b",
  "optionExplanations": {
    "a": {
      "text": "Incorrect. Operate does not offer a rename dialog on the variables panel; the documented step is to click the edit icon next to the variable and then save with the checkmark."
    },
    "b": {
      "text": "Correct. The documented steps are to click the edit icon next to the variable, edit the value (here by removing the quotation marks so it becomes an integer), and save by clicking the checkmark icon."
    },
    "c": {
      "text": "Incorrect. There is no script console for editing variables in Operate; values are typed directly into the variable row after clicking the edit icon next to the entry."
    },
    "d": {
      "text": "Incorrect. Operate does not expose a separate type switch for variables; the value is changed inline, which then determines the JSON type once the checkmark icon saves it."
    }
  },
  "explanation": "Resolving the incident in Operate involves clicking the edit icon next to the variable, editing the value, and clicking the checkmark icon to save. Removing the quotation marks converts the JSON string into an integer.",
  "docs": [
    {
      "title": "Diagnosing and resolving incidents",
      "url": "https://docs.camunda.io/docs/components/operate/userguide/resolve-incidents-update-variables/"
    }
  ]
}
