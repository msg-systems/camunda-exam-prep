{
  "id": "cfg-feel-built-in-functions-string-149",
  "topic": "configuring-processes",
  "subtopic": "feel-string-functions",
  "difficulty": "medium",
  "style": "concept",
  "camundaVersion": "8.8",
  "scenario": "Forge Bank breaks a CSV memo line into individual tokens for 16000 transactions per day in Camunda 8.8. A FEEL expression on an input mapping must split a string at every occurrence of a regular expression delimiter and pass the resulting list of substrings to the downstream worker.",
  "question": "What does the FEEL split function return when it is applied to a string and a delimiter pattern?",
  "options": [
    {
      "id": "a",
      "text": "It returns a single string with every match of the delimiter replaced by an empty value, preserving the original character count of the input string for auditing"
    },
    {
      "id": "b",
      "text": "It returns a list of substrings produced by splitting the input string at every match of the delimiter regular expression, as documented in the string functions reference"
    },
    {
      "id": "c",
      "text": "It returns the count of matches of the delimiter found inside the input string, which the caller can then use to size a downstream list of placeholders"
    },
    {
      "id": "d",
      "text": "It returns the first substring of the input that appears before the initial match of the delimiter and discards any text that follows the first occurrence"
    }
  ],
  "correctOptionId": "b",
  "optionExplanations": {
    "a": {
      "text": "Incorrect. The split function does not replace matches; it produces a list of substrings rather than a single modified string of equal length."
    },
    "b": {
      "text": "Correct. The documented split function returns a list of substrings produced by splitting the input at every match of the delimiter regular expression in line with the string functions reference."
    },
    "c": {
      "text": "Incorrect. The function does not return a count; it returns a list of substrings, and the caller can apply the list count function separately if the size is needed."
    },
    "d": {
      "text": "Incorrect. All segments are returned in the result list, not only the segment before the first delimiter match."
    }
  },
  "explanation": "FEEL split takes a string and a delimiter regular expression and returns a list of substrings produced by splitting the input at every match of the delimiter.",
  "docs": [
    {
      "title": "FEEL built-in functions string",
      "url": "https://docs.camunda.io/docs/components/modeler/feel/builtin-functions/feel-built-in-functions-string/"
    }
  ]
}
