{
  "id": "cfg-feel-built-in-functions-list-96",
  "topic": "configuring-processes",
  "subtopic": "feel-built-in-functions",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Forge Fintech screens 12000 transactions per day in Camunda 8.8 using FEEL on a business rule task. A FEEL expression must raise a fraud signal when at least one of several boolean indicators on a transaction is true, and the same expression must return false when the list of indicators on a transaction is empty.",
  "question": "Which FEEL function returns true if any element of a boolean list is true and returns false when the list is empty?",
  "options": [
    {
      "id": "a",
      "text": "all(list) returns true if any element is true and returns true when the input boolean list is empty for the transaction"
    },
    {
      "id": "b",
      "text": "or(list) returns true if any element is true and returns null when the input boolean list is empty for the transaction"
    },
    {
      "id": "c",
      "text": "any(list) returns true if any element is true and returns false when the input boolean list is empty for the transaction"
    },
    {
      "id": "d",
      "text": "some(list) returns true if any element is true and returns the first true element from the input boolean list"
    }
  ],
  "correctOptionId": "c",
  "optionExplanations": {
    "a": {
      "text": "Incorrect. all(list) returns false if any element is false and returns true for an empty list; it does not implement the requested fraud-signal semantics for at least one true element."
    },
    "b": {
      "text": "Incorrect. The function or() was the previous name and has been replaced by any(); it should no longer be used and is not the documented FEEL function in Camunda 8.8."
    },
    "c": {
      "text": "Correct. any(list) returns true if any element of the given list is true, otherwise returns false, and for an empty list it returns false, matching the fraud-signal requirement on a transaction."
    },
    "d": {
      "text": "Incorrect. There is no some(list) function in FEEL with this semantic, and any() returns a boolean result rather than the first matching element of the input list."
    }
  },
  "explanation": "any(list) returns true if any element of the given boolean list is true; otherwise it returns false, and for an empty list it returns false. It replaced the previous or() function, which should no longer be used.",
  "docs": [
    {
      "title": "FEEL built-in list functions",
      "url": "https://docs.camunda.io/docs/components/modeler/feel/builtin-functions/feel-built-in-functions-list/"
    }
  ]
}
