{
  "id": "cfg-process-instance-creation-57",
  "topic": "configuring-processes",
  "subtopic": "process-instance-creation",
  "difficulty": "easy",
  "style": "concept",
  "camundaVersion": "8.8",
  "scenario": "Quill Press, a publisher, is evaluating Camunda 8.8 tags as a way to carry routing keys across 900 instances per day in its editorial workflow. A developer asks what filtering semantics the search API uses when multiple tags are supplied together in a single query against the process instance endpoint.",
  "question": "What filtering semantics does the process instance search API use when multiple tags are supplied in a single query?",
  "options": [
    {
      "id": "a",
      "text": "AND semantics: an instance must contain every requested tag and may contain additional tags; substring matching and wildcard matching on tag values are unavailable in the search API."
    },
    {
      "id": "b",
      "text": "OR semantics: an instance is returned if it contains any one of the requested tags, and prefix wildcards on tag values are honored to widen results across each search query."
    },
    {
      "id": "c",
      "text": "XOR semantics: an instance is returned only when it contains exactly one of the requested tags, and tag casing is normalized to lowercase before comparison at search time."
    },
    {
      "id": "d",
      "text": "Exact-set semantics: an instance is returned only if its tag set equals the requested set, and any additional tags attached to the instance exclude it from the search results."
    }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": {
      "text": "Correct. Search filtering uses AND semantics: an instance must contain all requested tags, it may also contain additional tags, and partial or wildcard matching is not supported on tag values."
    },
    "b": {
      "text": "Incorrect. The documented semantics are AND across requested tags, not OR, and the search API does not support prefix wildcards for partial tag matching."
    },
    "c": {
      "text": "Incorrect. Tags are case-sensitive strings; the search API does not normalize casing, and the filter is AND across requested tags rather than XOR."
    },
    "d": {
      "text": "Incorrect. Additional tags on an instance do not exclude it from results; the filter only requires that the requested tags are all present on the instance."
    }
  },
  "explanation": "Tag search uses AND semantics: an instance must carry every requested tag but may carry more, and Camunda does not provide partial or wildcard matching on tag values in process instance queries.",
  "docs": [
    {
      "title": "Process instance creation",
      "url": "https://docs.camunda.io/docs/components/concepts/process-instance-creation/"
    }
  ]
}
