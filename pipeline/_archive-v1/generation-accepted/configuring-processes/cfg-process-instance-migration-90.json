{
  "id": "cfg-process-instance-migration-90",
  "topic": "configuring-processes",
  "subtopic": "process-instance-migration",
  "difficulty": "hard",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "A healthcare network must roll back 700 instances from version 14 to version 12 of the same bpmnProcessId after a defect is discovered in version 14. The platform team is unsure whether downgrading is even possible and whether a mapping from the source process id to the target process id is required for the migration plan to be valid.",
  "question": "Which statement correctly describes whether a process instance can be migrated from a newer version back to an older version of the same process definition?",
  "options": [
    {
      "id": "a",
      "text": "The process instance can be migrated to any deployed target definition, including an older version of the same bpmnProcessId, and no mapping is required from the source process id to the target process id"
    },
    {
      "id": "b",
      "text": "The process instance can be migrated only forward to a newer version of the same bpmnProcessId; downgrades require a cancel and recreate workflow because the engine refuses to migrate to an older version"
    },
    {
      "id": "c",
      "text": "The process instance can be migrated to an older version only if a process-id mapping is provided that maps the source bpmnProcessId to the target bpmnProcessId so the engine accepts the downgrade"
    },
    {
      "id": "d",
      "text": "The process instance can be migrated only to a different bpmnProcessId entirely; same-id downgrades are blocked by the validation step because the version property would otherwise move backward"
    }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": {
      "text": "Correct. A process instance can be migrated from an older version to a newer version of the same process definition, from a newer version back to an older one, or to a different process definition altogether. No mapping from the source process id to the target process id is required."
    },
    "b": {
      "text": "Incorrect. Migration is direction-agnostic with respect to versions of the same bpmnProcessId. The engine explicitly supports migrating from a newer version back to an older version without a cancel and recreate workflow."
    },
    "c": {
      "text": "Incorrect. Mapping instructions never need to map the source bpmnProcessId itself to the target bpmnProcessId. The process instance migration command does not include a process-id mapping concept."
    },
    "d": {
      "text": "Incorrect. Migration to a different bpmnProcessId is allowed, but it is not the only option. Migration within the same bpmnProcessId is supported in both directions, and the version property is updated as part of the migration step."
    }
  },
  "explanation": "Process instance migration can move an instance from an older to a newer version of the same definition, from newer to older of the same definition, or to a different process definition entirely. No mapping from the source process id to the target process id is needed.",
  "docs": [
    {
      "title": "Process instance migration",
      "url": "https://docs.camunda.io/docs/components/concepts/process-instance-migration/"
    }
  ]
}
