{
  "id": "ext-service-integration-patterns-155",
  "topic": "extensions-integrations",
  "subtopic": "Service task default",
  "difficulty": "easy",
  "style": "recall",
  "camundaVersion": "8.8",
  "scenario": "Helix Healthcare reviews 1800 integration calls per week in a Camunda 8.8 order process. The architect must record the documented default BPMN element to model both synchronous and asynchronous request/response interactions before the team starts modeling the next release of the patient onboarding flow.",
  "question": "Which BPMN element does Camunda recommend as the default for synchronous and asynchronous request/response calls?",
  "options": [
    {
      "id": "a",
      "text": "The service task is the documented default for both synchronous and asynchronous request/response calls because it removes visual clutter from the diagram for stakeholders"
    },
    {
      "id": "b",
      "text": "The send task is the documented default for both synchronous and asynchronous request/response calls because its envelope symbol makes integrations explicit on the diagram"
    },
    {
      "id": "c",
      "text": "A send task followed by a receive task is the documented default for both synchronous and asynchronous request/response calls in the integration patterns guidance"
    },
    {
      "id": "d",
      "text": "A message intermediate throw event paired with a receive task is the documented default for both synchronous and asynchronous request/response calls in Camunda 8.8"
    }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": {
      "text": "Correct. The Service task vs. send/receive task combo section recommends using the service task as the default option for synchronous and asynchronous request/response calls because it removes visual clutter for stakeholders."
    },
    "b": {
      "text": "Incorrect. Send tasks are recommended for fire-and-forget asynchronous messages, not as the default for request/response calls."
    },
    "c": {
      "text": "Incorrect. The send+receive task combo is recommended only when responses take longer (more than seconds) or when a self-controlled correlation ID is required."
    },
    "d": {
      "text": "Incorrect. The integration patterns chapter compares service tasks and send/receive tasks; it does not promote a throw+receive event pair as the default."
    }
  },
  "explanation": "Camunda 8 documents the service task as the default for both synchronous and asynchronous request/response interactions because it keeps the diagram free of visual clutter while still letting the engine wait for the response.",
  "docs": [
    {
      "title": "Service integration patterns",
      "url": "https://docs.camunda.io/docs/components/best-practices/development/service-integration-patterns/"
    }
  ]
}
