{
  "id": "ext-zeebe-api-rest-overview-108",
  "topic": "extensions-integrations",
  "subtopic": "zeebe-rest",
  "difficulty": "hard",
  "style": "concept",
  "camundaVersion": "8.8",
  "scenario": "Xenia Bank runs 2 Camunda 8.8 clusters with about 3500 process instances per day: a SaaS cluster for staging and a Self-Managed cluster behind an Ingress for production. A platform engineer is hardcoding the Zeebe REST base URLs for 4 internal tools and needs the exact context paths documented for v1 of the Zeebe REST API.",
  "question": "What are the documented v1 Zeebe REST API context paths for Camunda 8.8 SaaS and Self-Managed clusters?",
  "options": [
    {
      "id": "a",
      "text": "For SaaS the example path is https://${REGION}.zeebe.camunda.io:443/${CLUSTER_ID}/v1/ using the regionId and clusterId from the cluster API credentials, and for Self-Managed the default example path is http://localhost:8080/v1/"
    },
    {
      "id": "b",
      "text": "For SaaS the example path is https://zeebe.${CLUSTER_ID}.camunda.io/v1/ without a region segment, and for Self-Managed the path is fixed at https://zeebe.local:9600/v1/ regardless of the Zeebe Gateway Ingress configuration"
    },
    {
      "id": "c",
      "text": "For SaaS the example path is https://api.camunda.io/${CLUSTER_ID}/zeebe/v1/ embedded under a shared Console API host, and for Self-Managed the default example path is http://localhost:26500/v1/ using the Zeebe gRPC port"
    },
    {
      "id": "d",
      "text": "For SaaS the example path is https://${REGION}.camunda.io/${CLUSTER_ID}/zeebe/v1/ routed via the Camunda Console host, and for Self-Managed the default example path is http://localhost:9600/zeebe/v1/ on the actuator port"
    }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": {
      "text": "Correct. The Context paths section gives the SaaS example as https://${REGION}.zeebe.camunda.io:443/${CLUSTER_ID}/v1/ with the regionId and clusterId taken from the API tab credentials, and gives the Self-Managed default example as http://localhost:8080/v1/ matching the documented Zeebe Gateway Ingress setup."
    },
    "b": {
      "text": "Incorrect. The SaaS path is region-prefixed under zeebe.camunda.io with the cluster id in the path, and the Self-Managed default does not use a fixed zeebe.local:9600 host."
    },
    "c": {
      "text": "Incorrect. The SaaS path does not go through a shared api.camunda.io Console host, and the Self-Managed default example uses port 8080 rather than the 26500 gRPC port."
    },
    "d": {
      "text": "Incorrect. The SaaS path is under zeebe.camunda.io rather than the Console host, and the Self-Managed default example does not use the 9600 actuator port with a /zeebe prefix."
    }
  },
  "explanation": "The Camunda 8.8 Zeebe REST overview documents the SaaS example path as https://${REGION}.zeebe.camunda.io:443/${CLUSTER_ID}/v1/ using the regionId and clusterId from client credentials, and the Self-Managed default example path as http://localhost:8080/v1/ matching the Zeebe Gateway Ingress configuration.",
  "docs": [
    {
      "title": "Zeebe API (REST) overview",
      "url": "https://docs.camunda.io/docs/apis-tools/zeebe-api-rest/zeebe-api-rest-overview/"
    }
  ]
}
