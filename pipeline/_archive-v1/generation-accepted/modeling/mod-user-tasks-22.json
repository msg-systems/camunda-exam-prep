{
  "id": "mod-user-tasks-22",
  "topic": "modeling",
  "subtopic": "tasks",
  "difficulty": "medium",
  "style": "concept",
  "camundaVersion": "8.8",
  "scenario": "Polaris Insurer manages 5000 user tasks per day in Camunda 8.8 with SLAs that require both a follow-up date and a due date on the same task. A modeler wants to compute the due date dynamically as now() plus 15 seconds and needs to confirm exactly when the expression is evaluated and what result type the engine expects.",
  "question": "Which rule governs the dueDate and followUpDate attributes of the zeebe:taskSchedule extension element?",
  "options": [
    {
      "id": "a",
      "text": "Both attributes accept static values or FEEL expressions, evaluated on activating the user task and producing a string in ISO 8601 combined date and time format"
    },
    {
      "id": "b",
      "text": "Both attributes accept only static ISO 8601 strings defined at design time, with FEEL expressions treated as disallowed for taskSchedule attribute values"
    },
    {
      "id": "c",
      "text": "Both attributes accept FEEL expressions evaluated at process deployment time and the result must be a number of milliseconds representing the relative deadline"
    },
    {
      "id": "d",
      "text": "Both attributes accept FEEL expressions evaluated on activating the user task and the result must be a list of ISO 8601 strings representing alternative candidate dates"
    }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": {
      "text": "Correct. The dueDate and followUpDate attributes of the zeebe:taskSchedule element accept static values or FEEL expressions. Expressions are evaluated on activating the user task and must result in a string conforming to an ISO 8601 combined date and time representation."
    },
    "b": {
      "text": "Incorrect. FEEL expressions such as equals now() plus a duration are explicitly supported on the dueDate and followUpDate attributes, so static-only is not the rule."
    },
    "c": {
      "text": "Incorrect. The expressions are evaluated on activating the user task rather than at deployment, and the required result is an ISO 8601 string rather than a number of milliseconds."
    },
    "d": {
      "text": "Incorrect. The result of the expression must be a single ISO 8601 string for each attribute, not a list of alternative date strings."
    }
  },
  "explanation": "The zeebe:taskSchedule element on a user task exposes dueDate and followUpDate attributes. Each can be a static value or a FEEL expression evaluated on activating the user task, and the result must be an ISO 8601 combined date and time string.",
  "docs": [
    {
      "title": "BPMN user tasks",
      "url": "https://docs.camunda.io/docs/components/modeler/bpmn/user-tasks/"
    }
  ]
}
