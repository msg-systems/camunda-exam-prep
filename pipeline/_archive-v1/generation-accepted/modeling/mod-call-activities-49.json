{
  "id": "mod-call-activities-49",
  "topic": "modeling",
  "subtopic": "subprocesses",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Wren Telco's Camunda 8.8 service activation flow, handling 12000 customers per day across 32 tenants, invokes a regional shipping process whose BPMN process id is computed at runtime from the tenant identifier. The architect needs to know which call activity attribute carries that expression and what kind of value it must produce.",
  "question": "Which processId configuration on a call activity supports computing the called process at runtime from a tenant identifier?",
  "options": [
    {
      "id": "a",
      "text": "The processId attribute carries a FEEL expression such as = \"shipping-\" + tenantId, evaluated on activation, which must result in a string naming the called process"
    },
    {
      "id": "b",
      "text": "The processId attribute carries a JSON object whose tenantId key is resolved by the engine before activation, and the string value of that key is used as the process id"
    },
    {
      "id": "c",
      "text": "The processId attribute carries a path to a deployed BPMN file, and the engine resolves the tenantId by reading variables from the file's first start event configuration"
    },
    {
      "id": "d",
      "text": "The processId attribute carries a regular expression that matches a list of deployed BPMN ids, and the engine selects whichever process id matches the running tenant value"
    }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": {
      "text": "Correct. The processId attribute can be a static value or a FEEL expression evaluated on activation; the expression must result in a string that names the called BPMN process."
    },
    "b": {
      "text": "Incorrect. The processId attribute does not carry a JSON object; it carries either a static string or a FEEL expression resolving to a string."
    },
    "c": {
      "text": "Incorrect. The processId attribute is not a path to a BPMN file; it references a process by BPMN process id, not by file system location."
    },
    "d": {
      "text": "Incorrect. The processId attribute is not a regular expression matcher; it carries a literal id or a FEEL expression that resolves to one."
    }
  },
  "explanation": "The processId attribute on a call activity can be a static value or a FEEL expression. The expression is evaluated on activation and must return a string that names the called BPMN process.",
  "docs": [
    {
      "title": "BPMN call activities",
      "url": "https://docs.camunda.io/docs/components/modeler/bpmn/call-activities/"
    }
  ]
}
