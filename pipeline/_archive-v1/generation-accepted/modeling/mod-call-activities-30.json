{
  "id": "mod-call-activities-30",
  "topic": "modeling",
  "subtopic": "call-activities",
  "difficulty": "easy",
  "style": "concept",
  "camundaVersion": "8.8",
  "scenario": "Aurora Retail orchestrates an order workflow in Camunda 8.8 with a call activity that launches a child shipping process for 9000 orders per day. The team wants the call activity to always instantiate the most recent deployed version of the shipping process so that improvements take effect on the next process instance without redeploying the parent.",
  "question": "Which bindingType value on the call activity always instantiates the most recent deployed version of the called process?",
  "options": [
    {
      "id": "a",
      "text": "The bindingType set to latest instantiates the most recent deployed version of the called process at the moment of call activity activation"
    },
    {
      "id": "b",
      "text": "The bindingType set to deployment instantiates the version of the called process that was deployed together with the calling process definition"
    },
    {
      "id": "c",
      "text": "The bindingType set to versionTag instantiates the latest deployed version annotated with the version tag configured on the call activity"
    },
    {
      "id": "d",
      "text": "The bindingType set to current instantiates the draft version of the called process held in the connected Web Modeler workspace"
    }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": {
      "text": "Correct. The latest bindingType resolves the called process to the most recent deployed version when the call activity is activated. This value is also the implicit default if the attribute is omitted."
    },
    "b": {
      "text": "Incorrect. The deployment bindingType pins the called process to the version that was deployed together with the calling process. Newer versions of the called process would not be used."
    },
    "c": {
      "text": "Incorrect. The versionTag bindingType selects the latest deployment annotated with a specific tag value. Every new release would have to carry that tag for it to be picked up."
    },
    "d": {
      "text": "Incorrect. There is no current bindingType for call activities. The documented values are latest, deployment, and versionTag."
    }
  },
  "explanation": "A call activity selects which version of the called process to instantiate through the bindingType attribute. Setting it to latest, which is also the implicit default when the attribute is omitted, always picks the most recent deployed version at activation time.",
  "docs": [
    {
      "title": "BPMN call activities",
      "url": "https://docs.camunda.io/docs/components/modeler/bpmn/call-activities/"
    }
  ]
}
