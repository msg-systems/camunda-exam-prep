{
  "id": "mod-manual-tasks-28",
  "topic": "modeling",
  "subtopic": "tasks",
  "difficulty": "medium",
  "style": "concept",
  "camundaVersion": "8.8",
  "scenario": "Granite Manufacturing, a manufacturer of industrial valves, tracks 1500 tasks per week of preventive maintenance in Camunda 8.8. The process model represents a physical inspection step performed offline by a technician, with no UI or worker integration, so the modeller chooses a BPMN manual task to document that step inside the flow.",
  "question": "Which runtime behaviour does the Camunda 8.8 engine apply to a BPMN manual task element when the process instance reaches it?",
  "options": [
    {
      "id": "a",
      "text": "The engine creates a user task entry in Tasklist for the manual task and waits for a human to complete it through Tasklist."
    },
    {
      "id": "b",
      "text": "The engine activates a job worker subscription using the configured taskDefinition type and waits for a worker to complete the job."
    },
    {
      "id": "c",
      "text": "The engine blocks the manual task until an external client calls a complete-manual-task API exposed by the Zeebe gateway to the team."
    },
    {
      "id": "d",
      "text": "The engine treats the manual task as a pass-through activity that completes automatically the moment the process instance arrives at it."
    }
  ],
  "correctOptionId": "d",
  "optionExplanations": {
    "a": {
      "text": "Incorrect. A manual task does not generate a Tasklist entry; user tasks are the actionable BPMN element assisted by Tasklist, while manual tasks are documentation-only and behave as a pass-through inside the engine."
    },
    "b": {
      "text": "Incorrect. Manual tasks have no taskDefinition type and no job worker subscription; that mechanism is used by service tasks, not by manual tasks, which the engine completes automatically."
    },
    "c": {
      "text": "Incorrect. There is no complete-manual-task API; manual tasks are handled as pass-through activities and the engine continues the process immediately, without any external completion call."
    },
    "d": {
      "text": "Correct. The documentation states that the engine handles a manual task as a pass-through activity, automatically continuing the process at the moment the process instance arrives at the task."
    }
  },
  "explanation": "A BPMN manual task models a step performed by a human outside the process engine, such as a physical inspection. In the engine it behaves as a pass-through activity that automatically continues the process the moment the instance arrives at the task.",
  "docs": [
    {
      "title": "BPMN manual task",
      "url": "https://docs.camunda.io/docs/components/modeler/bpmn/manual-tasks/"
    }
  ]
}
