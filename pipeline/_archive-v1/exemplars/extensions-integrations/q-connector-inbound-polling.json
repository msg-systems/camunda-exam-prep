{
  "id": "ext-connector-inbound-polling",
  "topic": "extensions-integrations",
  "subtopic": "Connector SDK",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Aurora Telco polls an external REST endpoint to drive a Camunda 8.8 process for 1500 events per day. The integration must run an inbound connector that periodically calls the configured URL and publishes a Zeebe message when the response shape matches a configured condition expression.",
  "question": "Which Camunda 8 inbound connector pattern handles periodic polling of a remote endpoint?",
  "options": [
    { "id": "a", "text": "Use the REST inbound polling connector; the runtime schedules the configured URL fetch and publishes a message on a matching condition expression" },
    { "id": "b", "text": "Use the Webhook inbound connector with a scheduler header; the runtime self-invokes the webhook URL on a cron schedule and publishes a message on each fire" },
    { "id": "c", "text": "Use the Kafka inbound connector with a poll mode flag; the runtime reuses the Kafka subscriber as a REST poller when poll mode is enabled on the connector" },
    { "id": "d", "text": "Use a custom outbound connector wrapped in a timer event loop; the modeler combines the timer and outbound connector to simulate periodic inbound polling" }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": { "text": "Correct. The REST inbound polling connector schedules the configured URL fetch and publishes a Zeebe message on a matching condition expression." },
    "b": { "text": "Incorrect. The Webhook connector is event-driven HTTP, not polling; polling is a separate inbound type." },
    "c": { "text": "Incorrect. Kafka inbound is a Kafka subscriber and does not double as a REST poller." },
    "d": { "text": "Incorrect. There is a dedicated inbound polling connector; combining timer plus outbound is unnecessary." }
  },
  "explanation": "Camunda 8 provides an inbound REST polling connector; the runtime schedules the URL fetch and publishes a Zeebe message on a matching condition expression.",
  "docs": [
    { "title": "REST polling inbound connector", "url": "https://docs.camunda.io/docs/components/connectors/protocol/polling/" }
  ]
}
