{
  "id": "mod-call-activity-vs-embedded",
  "topic": "modeling",
  "subtopic": "subprocesses",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Pinnacle Bank reuses a credit-check sub-process across 8 parent processes in Camunda 8.8 totaling 11000 loans per day. The team needs the sub-process to live as a separate deployable BPMN file with its own version history and to be invokable from every parent process by its process definition id.",
  "question": "Which BPMN element invokes a separately deployed process definition as a child instance with its own lifecycle?",
  "options": [
    { "id": "a", "text": "Call activity references another deployed process definition by id and creates a separate child process instance with its own lifecycle" },
    { "id": "b", "text": "Embedded subprocess references another deployed process definition by id and creates a separate child process instance with its own lifecycle" },
    { "id": "c", "text": "Service task with a special call job type references a deployed process definition by id and creates a separate child process instance" },
    { "id": "d", "text": "Ad-hoc subprocess references another deployed process definition by id and creates a separate child process instance for each activation" }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": { "text": "Correct. Call activities invoke a separately deployed process definition by id; the child runs as its own process instance with an independent lifecycle." },
    "b": { "text": "Incorrect. Embedded subprocesses are inline within the same BPMN file; they do not reference a separately deployed process definition." },
    "c": { "text": "Incorrect. There is no call-typed service task in Camunda 8; invoking another process is the role of the call activity element." },
    "d": { "text": "Incorrect. Ad-hoc subprocesses contain unordered inner activities within the same definition and do not call out to another process." }
  },
  "explanation": "Call activities reference a separately deployed process definition by id and create an independent child process instance, which is the reuse pattern across parents.",
  "docs": [
    { "title": "BPMN call activity", "url": "https://docs.camunda.io/docs/components/modeler/bpmn/call-activities/" }
  ]
}
