{
  "id": "mod-message-events-84",
  "topic": "modeling",
  "subtopic": "events",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Iris Telco wires 6000 events per day from boundary message catch events on activities running in Camunda 8.8. The platform team is reviewing the BPMN XML and must place the correlationKey expression in the exact element required by the documented schema.",
  "question": "Where is the correlationKey expression declared in the BPMN XML for an intermediate message catch event?",
  "options": [
    {
      "id": "a",
      "text": "The correlationKey is declared as a direct XML attribute on the bpmn:intermediateCatchEvent element alongside the messageRef pointing at the bpmn:message definition"
    },
    {
      "id": "b",
      "text": "The correlationKey is declared on the outer bpmn:process element so every catching message event in the process shares a single inherited correlationKey expression"
    },
    {
      "id": "c",
      "text": "The correlationKey is declared on the bpmn:messageEventDefinition element as a child text node alongside the messageRef pointing at the bpmn:message definition"
    },
    {
      "id": "d",
      "text": "The correlationKey is declared on a zeebe:subscription element nested under bpmn:extensionElements inside the bpmn:message definition referenced by the catch event"
    }
  ],
  "correctOptionId": "d",
  "optionExplanations": {
    "a": {
      "text": "Incorrect. The documented XML places the correlationKey on a zeebe:subscription extension under the bpmn:message, not as an attribute on the intermediate catch event element itself."
    },
    "b": {
      "text": "Incorrect. The correlationKey is per message definition and is not inherited from the surrounding bpmn:process element in the documented XML representation."
    },
    "c": {
      "text": "Incorrect. The documented schema does not use a child text node on bpmn:messageEventDefinition for correlation; the correlationKey lives under a zeebe:subscription on the message."
    },
    "d": {
      "text": "Correct. The documented XML for an intermediate message catch event places the correlationKey expression on a zeebe:subscription nested under bpmn:extensionElements within the bpmn:message that the catch event references."
    }
  },
  "explanation": "The Additional resources XML shows the correlationKey as an attribute of zeebe:subscription, nested under bpmn:extensionElements inside the bpmn:message definition that the catch event references via messageRef.",
  "docs": [
    {
      "title": "BPMN message events",
      "url": "https://docs.camunda.io/docs/components/modeler/bpmn/message-events/"
    }
  ]
}
