{
  "id": "dec-decision-requirements-graph-43",
  "topic": "decisions-business-rules",
  "subtopic": "decision-requirements-graph",
  "difficulty": "medium",
  "style": "concept",
  "camundaVersion": "8.8",
  "scenario": "Helios Energy serves 200000 customers and models a Camunda 8.8 tariff DRG that includes an inputData node named 'Customer tariff plan' wired to several depending decisions. A new modeler is debugging an evaluation incident and wonders what role inputData actually plays when the engine evaluates the DRG.",
  "question": "What is the runtime role of an inputData element when a Camunda 8.8 DRG is evaluated by the engine?",
  "options": [
    {
      "id": "a",
      "text": "An inputData element has no execution semantics in a DRG and is ignored by the engine when the depending decisions are evaluated"
    },
    {
      "id": "b",
      "text": "An inputData element is fetched by the engine from process variables and merged into the inputs of every depending decision"
    },
    {
      "id": "c",
      "text": "An inputData element raises an evaluation incident when the matching process variable cannot be resolved at decision time"
    },
    {
      "id": "d",
      "text": "An inputData element is treated as a required decision whose value is computed first and cached for the depending decisions"
    }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": {
      "text": "Correct. The docs explicitly note that an inputData element has no execution semantics and is ignored on evaluation, so it is purely a modelling annotation rather than a runtime input source."
    },
    "b": {
      "text": "Incorrect. The engine does not auto-fetch values for inputData; inputs to a decision are bound through the decision's own input expressions, not the inputData node."
    },
    "c": {
      "text": "Incorrect. An ignored model element cannot raise an evaluation incident; missing variables surface through the decision's input expression evaluation instead."
    },
    "d": {
      "text": "Incorrect. An inputData node is not a decision and has no decision logic, so the engine never evaluates it as if it were a required decision."
    }
  },
  "explanation": "inputData is a documentation-only DRG construct. The Camunda DMN engine ignores it at evaluation time and never derives runtime values from it. Inputs to a decision are supplied through the decision's own input expressions, not through any inputData reference.",
  "docs": [
    {
      "title": "Decision requirements graph",
      "url": "https://docs.camunda.io/docs/components/modeler/dmn/decision-requirements-graph/"
    }
  ]
}
