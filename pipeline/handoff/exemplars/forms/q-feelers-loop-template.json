{
  "id": "frm-feelers-loop-template",
  "topic": "forms",
  "subtopic": "templating syntax",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Pinnacle Retail, an e-commerce merchant, processes 8000 orders per day with an average basket of $120 across up to 20 line items. A Camunda 8.8 form text block renders the order summary, and each line must read \"Item name: <name>, Item price: $<price>\" without writing per-row markup in the form schema.",
  "question": "Which feelers template construct iterates over the items array and renders one line per element?",
  "options": [
    { "id": "a", "text": "Wrap the per-line markup in {{#loop items}} and {{/loop}} tags, referencing {{name}} and {{price}} inside for each array element" },
    { "id": "b", "text": "Use a single {{items}} insert; the form runtime auto-expands arrays by joining each element with a newline at render time" },
    { "id": "c", "text": "Add a JavaScript for-loop inside the template body and reference items by index via embedded script blocks for client-side iteration" },
    { "id": "d", "text": "Use repeated {{#if items}} blocks, one per index up to a fixed upper bound such as 20 rows, to render each element separately" }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": { "text": "Correct. The feelers loop construct {{#loop items}} ... {{/loop}} iterates over arrays, exposing element fields directly such as {{name}} and {{price}}." },
    "b": { "text": "Incorrect. A bare {{items}} insert renders the FEEL expression once; feelers does not auto-expand arrays into lines without the loop tag." },
    "c": { "text": "Incorrect. Feelers is its own template syntax and does not embed JavaScript; iteration is expressed exclusively with the loop tag." },
    "d": { "text": "Incorrect. The if tag conditionally renders a section; iteration over an array of unknown length is what the loop tag is designed for." }
  },
  "explanation": "Feelers templates iterate over arrays using {{#loop items}} ... {{/loop}}. Inside the loop, element properties are referenced directly or via {{this}} for primitive arrays.",
  "docs": [
    { "title": "Forms templating syntax - iterating through arrays", "url": "https://docs.camunda.io/docs/components/modeler/forms/configuration/forms-config-templating-syntax/" }
  ]
}
