{
  "id": "dec-decision-table-output-27",
  "topic": "decisions-business-rules",
  "subtopic": "decision-table-output",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Forge Lender runs a Camunda 8.8 DMN table that returns 3 outputs per matched rule across 9500 loans per day in its credit decisioning pipeline. A reviewer asks the modelers to confirm how each output clause is represented in the DMN XML and whether a single decision table is allowed to declare more than one output clause in Camunda 8.8.",
  "question": "Where is each output clause defined in the DMN XML and how many output clauses may a Camunda 8.8 decision table declare?",
  "options": [
    {
      "id": "a",
      "text": "Each output clause is an output element nested directly inside the decisionTable element, and the table may declare one or more output clauses"
    },
    {
      "id": "b",
      "text": "Each output clause is an outputClause element nested inside the decision element, and the table is restricted to exactly one output clause per decision"
    },
    {
      "id": "c",
      "text": "Each output clause is an output element nested directly inside the definitions root element, and the table may declare at most two output clauses per decision"
    },
    {
      "id": "d",
      "text": "Each output clause is an output element nested inside the inputExpression element, and the table may declare any number of clauses per rule in Camunda 8.8"
    }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": {
      "text": "Correct. The docs show output elements as direct children of decisionTable and state that a decision table may have one or more output clauses in Camunda 8.8."
    },
    "b": {
      "text": "Incorrect. The element name is output, not outputClause, and the schema explicitly allows more than one output clause rather than restricting the table to one."
    },
    "c": {
      "text": "Incorrect. Output clauses sit inside the decisionTable element, not directly under the definitions root, and there is no documented two clause limit."
    },
    "d": {
      "text": "Incorrect. The output element is nested in decisionTable, not inside an inputExpression, which is part of the input clause structure rather than the output side."
    }
  },
  "explanation": "A Camunda 8.8 DMN decision table represents each output clause as an output element nested directly under decisionTable, and a table may declare one or more output clauses per decision.",
  "docs": [
    {
      "title": "DMN decision table output",
      "url": "https://docs.camunda.io/docs/components/modeler/dmn/decision-table-output/"
    }
  ]
}
