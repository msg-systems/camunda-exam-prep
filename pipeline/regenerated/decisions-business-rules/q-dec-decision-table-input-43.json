{
  "id": "dec-decision-table-input-43",
  "topic": "decisions-business-rules",
  "subtopic": "input",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Lumen airline runs a Camunda 8.8 DMN seat-pricing table for 9000 customers per day. A modeler adds a Season input column that references a process variable named season already populated by the calling process. The reviewer asks which expression language the modeler types into the input column and where exactly that text is placed in the XML.",
  "question": "Which expression language is used for the input expression and where is the expression text placed in the XML?",
  "options": [
    {
      "id": "a",
      "text": "The expression is written in JUEL and the text is placed in the text content of the inputExpression XML element nested inside the input element of the decisionTable"
    },
    {
      "id": "b",
      "text": "The expression is written in FEEL and the text is placed in the label attribute of the input XML element nested inside the decisionTable element of the deployed DMN file"
    },
    {
      "id": "c",
      "text": "The expression is written in FEEL and the text is placed in the text content of the inputExpression XML element nested inside the input element of the decisionTable"
    },
    {
      "id": "d",
      "text": "The expression is written in Groovy and the text is placed in the typeRef attribute of the inputExpression XML element nested inside the input element of the decisionTable"
    }
  ],
  "correctOptionId": "c",
  "optionExplanations": {
    "a": {
      "text": "Incorrect. JUEL is the Camunda 7 expression language; the Input expression section explicitly identifies FEEL as the expression language for the input expression on the Camunda 8 DMN decision table."
    },
    "b": {
      "text": "Incorrect. The label attribute on the input element carries a short description of the input, not the FEEL expression text; the expression text lives in the text element nested inside inputExpression."
    },
    "c": {
      "text": "Correct. The Input expression section states that the expression language of the input expression is FEEL and that the expression is set inside a text element that is a child of the inputExpression XML element."
    },
    "d": {
      "text": "Incorrect. Groovy is not used by Camunda 8 for DMN input expressions, and the typeRef attribute carries the input type definition rather than the FEEL expression text used to read the input value."
    }
  },
  "explanation": "The Input expression section says the expression language is FEEL and that the expression is set inside a text element that is a child of the inputExpression XML element. The input expression typically references a process variable available during the evaluation of the decision table.",
  "docs": [
    {
      "title": "DMN decision table input",
      "url": "https://docs.camunda.io/docs/components/modeler/dmn/decision-table-input/"
    }
  ]
}
