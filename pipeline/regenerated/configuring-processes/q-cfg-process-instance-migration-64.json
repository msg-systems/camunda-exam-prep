{
  "id": "cfg-process-instance-migration-64",
  "topic": "configuring-processes",
  "subtopic": "migration",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Lattice Bank runs 9000 instances of a loan onboarding process on Camunda 8.8. The original model uses an embedded subprocess, a call activity, and a parallel multi-instance service task. The operations team wants to roll out a redesigned definition using the process instance migration tool and needs to confirm which of the elements they have actually fall inside the supported BPMN element set listed in the docs.",
  "question": "Which set of in-flight elements in the Lattice Bank loan process is covered by the migration tool's supported BPMN elements list?",
  "options": [
    {
      "id": "a",
      "text": "The embedded subprocess, the call activity, and the parallel multi-instance service task are all listed under supported subprocesses, tasks, and markers for the migration tool"
    },
    {
      "id": "b",
      "text": "The embedded subprocess and call activity are supported but parallel multi-instance markers are explicitly excluded from the supported markers section for the migration tool"
    },
    {
      "id": "c",
      "text": "Only the service task is supported because subprocesses and call activities are documented as unsupported by the migration tool for in-flight token migration"
    },
    {
      "id": "d",
      "text": "The call activity and multi-instance marker are supported but embedded subprocesses fall outside the supported subprocess types listed by the migration tool"
    }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": {
      "text": "Correct. The supported BPMN elements section lists embedded subprocesses, call activities, and event subprocesses under subprocesses; service tasks under tasks; and both parallel and sequential multi-instance markers, so all three Lattice Bank elements are covered."
    },
    "b": {
      "text": "Incorrect. The markers section lists both parallel and sequential multi-instance as supported, so parallel multi-instance is not excluded from the migration tool's coverage."
    },
    "c": {
      "text": "Incorrect. Embedded subprocesses and call activities are both listed as supported subprocess types for the migration tool, alongside event subprocesses, in the docs."
    },
    "d": {
      "text": "Incorrect. Embedded subprocesses appear in the supported subprocesses list together with call activities and event subprocesses, so they are inside the migration tool's coverage."
    }
  },
  "explanation": "The process instance migration page lists supported elements by group. Subprocesses include embedded, call activity, and event subprocess; tasks include service, user, receive, send, business rule, and script; markers cover parallel and sequential multi-instance, so a process built from these is fully covered.",
  "docs": [
    {
      "title": "Process instance migration",
      "url": "https://docs.camunda.io/docs/components/concepts/process-instance-migration/"
    }
  ]
}
