{
  "id": "mng-resolve-incidents-update-variables-27",
  "topic": "managing-development",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "docs": [
    {
      "title": "Resolve incidents and update variables",
      "url": "https://docs.camunda.io/docs/guides/getting-started-orchestration-cluster/resolve-incidents-update-variables/"
    }
  ],
  "subtopic": "Operate incidents",
  "scenario": "A Zenith Bank developer follows the Camunda 8.8 getting-started guide on a Windows laptop while exploring 4 instances. The developer needs the equivalent of the Linux worker handler that the guide demonstrates for completing the Initiate Payment service task on the order-process instance using a community zbctl binary.",
  "question": "Which Windows zbctl invocation does the guide use to complete the Initiate Payment service task on the instance?",
  "options": [
    {
      "id": "a",
      "text": "The guide runs the Windows invocation .\\bin\\zbctl.exe --insecure create worker initiate-payment --handler findstr .*, which starts a community zbctl worker using findstr as its job handler on Windows"
    },
    {
      "id": "b",
      "text": "The guide runs powershell.exe -File complete-payment.ps1 to call into Camunda Operate over HTTPS, which then marks the Initiate Payment service task as done on the order-process instance"
    },
    {
      "id": "c",
      "text": "The guide runs .\\bin\\zbctl.exe --secure create worker initiate-payment --handler cat from a WSL bash shell;  the documented Windows flow simply reuses the Linux cat handler unchanged"
    },
    {
      "id": "d",
      "text": "The guide runs .\\bin\\zbctl.exe --insecure run worker initiate-payment without any --handler value;  on Windows the worker defaults to a no-op handler that completes Initiate Payment jobs automatically"
    }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": {
      "text": "Correct. The Windows tab in the guide shows .\\bin\\zbctl.exe --insecure create worker initiate-payment --handler findstr .*, which starts a community zbctl worker using findstr as the Windows equivalent of the Linux cat handler for Initiate Payment."
    },
    "b": {
      "text": "Incorrect. The guide drives Initiate Payment with a community zbctl worker rather than a PowerShell script that calls into Operate over HTTPS to mark the service task as done."
    },
    "c": {
      "text": "Incorrect. The Windows tab uses zbctl.exe with the findstr handler rather than reusing the Linux cat handler from a WSL bash shell with the --secure flag."
    },
    "d": {
      "text": "Incorrect. The Windows command keeps an explicit --handler findstr .* value rather than a no-op default, and the worker action is create worker rather than run worker on the binary."
    }
  },
  "explanation": "On Windows the guide starts the Initiate Payment worker with .\\bin\\zbctl.exe --insecure create worker initiate-payment --handler findstr .*. The findstr .* shell command is the Windows equivalent of the cat handler used on Linux and macOS in the guide."
}
