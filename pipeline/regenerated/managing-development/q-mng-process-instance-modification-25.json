{
  "id": "mng-process-instance-modification-25",
  "topic": "managing-development",
  "subtopic": "Operate",
  "difficulty": "easy",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "An operator at Lattice Insurance is repairing a Camunda 8.8 process instance handling 800 claims per day, after a pending Add modification created a brand new subprocess scope on the instance. The operator needs to seed that fresh scope with starter variables in the next 15 minutes before applying all modifications, so downstream service tasks have the required inputs when execution resumes.",
  "question": "Which sequence in Operate adds variables to the newly generated scope as pending modifications?",
  "options": [
    {
      "id": "a",
      "text": "Click Add Variable in the diagram toolbar, type the variable name and value, and the variable is queued as a pending modification on the parent process instance root scope of the instance"
    },
    {
      "id": "b",
      "text": "Open the variables panel on the root process scope, type the variable name and value, and the variable is queued as a pending modification on every nested running scope of the instance"
    },
    {
      "id": "c",
      "text": "Select the new scope in the instance history, click Edit Variable in the variables panel, type the name and value, and the variable is queued as a pending modification on that new scope"
    },
    {
      "id": "d",
      "text": "Select the new scope in the instance history, click Add Variable in the variables panel, type the Name and Value, and on blur the variable becomes a pending modification on the new scope"
    }
  ],
  "correctOptionId": "d",
  "optionExplanations": {
    "a": {
      "text": "Incorrect. The Add Variable action lives in the variables panel of the selected scope, not in a diagram toolbar. It targets the chosen new scope rather than the root scope of the process instance."
    },
    "b": {
      "text": "Incorrect. Variables are added to a specific selected scope from the variables panel of that scope. Opening the root scope does not seed every nested scope at once; each scope is targeted by its own Add Variable action."
    },
    "c": {
      "text": "Incorrect. Edit Variable changes the value of an existing variable. The action documented for seeding a freshly generated scope with a new Name and Value is Add Variable in the variables panel."
    },
    "d": {
      "text": "Correct. With modification mode active the operator selects the new scope from the instance history, clicks Add Variable in the variables panel, fills Name and Value, and on blur with valid values the variable is queued as a pending modification on that scope."
    }
  },
  "explanation": "To add variables to a newly generated scope, the operator selects the scope in the instance history, clicks Add Variable in the variables panel, fills the Name and Value fields, and on blur with valid values the variable is added to the pending modifications.",
  "docs": [
    {
      "title": "Process instance modification",
      "url": "https://docs.camunda.io/docs/components/operate/userguide/process-instance-modification/"
    }
  ]
}
