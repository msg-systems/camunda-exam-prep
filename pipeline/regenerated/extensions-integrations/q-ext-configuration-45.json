{
  "id": "ext-configuration-45",
  "topic": "extensions-integrations",
  "subtopic": "spring-boot-starter",
  "difficulty": "easy",
  "style": "concept",
  "camundaVersion": "8.8",
  "scenario": "Orion Bank operates a Camunda Spring Boot Starter worker on Camunda 8.8 that drives 7600 transactions per hour through the Zeebe gateway. The team currently lets the client choose its transport but performance tests show that the gRPC channel reduces tail latency for their workload. A reviewer wants to confirm which property flips the default behaviour from REST to gRPC.",
  "question": "Which property change forces the Camunda client to use gRPC instead of REST as the default transport?",
  "options": [
    {
      "id": "a",
      "text": "Set camunda.client.prefer-rest-over-grpc to true so that the Camunda client uses gRPC to communicate with the Camunda APIs by default for every operation in the worker process"
    },
    {
      "id": "b",
      "text": "Set camunda.client.transport to grpc so that the Camunda client uses gRPC to communicate with the Camunda APIs by default for every operation in the worker process"
    },
    {
      "id": "c",
      "text": "Set camunda.client.prefer-rest-over-grpc to false so that the Camunda client uses gRPC to communicate with the Camunda APIs by default for every operation in the worker process"
    },
    {
      "id": "d",
      "text": "Set camunda.client.use-grpc-by-default to true so that the Camunda client uses gRPC to communicate with the Camunda APIs by default for every operation in the worker process"
    }
  ],
  "correctOptionId": "c",
  "optionExplanations": {
    "a": {
      "text": "Incorrect. The property name is prefer-rest-over-grpc; setting it to true keeps REST as the default, since true means preferring REST."
    },
    "b": {
      "text": "Incorrect. The documented property is camunda.client.prefer-rest-over-grpc, not camunda.client.transport; there is no transport property listed in the connectivity section."
    },
    "c": {
      "text": "Correct. The docs state that by default the client uses REST whenever possible, and that to use gRPC by default you configure camunda.client.prefer-rest-over-grpc to false."
    },
    "d": {
      "text": "Incorrect. There is no use-grpc-by-default property in the connectivity section; the documented flag is camunda.client.prefer-rest-over-grpc."
    }
  },
  "explanation": "The connectivity section documents camunda.client.prefer-rest-over-grpc. By default the client uses REST whenever possible; setting prefer-rest-over-grpc to false makes the Camunda client use gRPC by default to communicate with the Camunda APIs.",
  "docs": [
    {
      "title": "Camunda Spring Boot Starter configuration",
      "url": "https://docs.camunda.io/docs/apis-tools/camunda-spring-boot-starter/configuration/"
    }
  ]
}
