{
  "id": "mod-message-events-54",
  "topic": "modeling",
  "subtopic": "message-events",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Catalyst Bank correlates 3000 invoices per day in Camunda 8.8 to long-running loan processes through an intermediate message catch event named Money collected, with each instance tracking its loan via an orderId variable that the merchant publishes with every message arriving from the payments rail.",
  "question": "What must the referenced message define on an intermediate message catch event in Camunda 8?",
  "options": [
    {
      "id": "a",
      "text": "A name plus a correlationKey expression evaluated on activation that must resolve to either a string or a number value"
    },
    {
      "id": "b",
      "text": "A name alone since the correlationKey expression is optional on every intermediate message catch event in the model"
    },
    {
      "id": "c",
      "text": "A name plus a correlationKey expression evaluated at deploy time that must resolve to a Boolean value of true at runtime"
    },
    {
      "id": "d",
      "text": "A name plus a correlationKey expression evaluated on activation that must resolve to a context with a fixed orderId key"
    }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": {
      "text": "Correct. The Messages section states a message must define a name and a correlationKey expression that is evaluated on activating the message event and must result in either a string or a number."
    },
    "b": {
      "text": "Incorrect. The correlationKey is only optional when the message is referenced solely by message start events; an intermediate catch event references the message and requires the correlationKey expression."
    },
    "c": {
      "text": "Incorrect. The Messages section says the correlationKey expression is evaluated on activation of the message event for non-start events, and the result must be a string or number, not a Boolean true."
    },
    "d": {
      "text": "Incorrect. The correlationKey expression must resolve to a string or number value such as orderId, not to a context map containing keys; only the result value is used for correlation."
    }
  },
  "explanation": "An intermediate message catch event references a message that requires a name plus a correlationKey expression, which the engine evaluates on activation and which must yield a string or numeric value used to match an incoming message.",
  "docs": [
    {
      "title": "BPMN message events",
      "url": "https://docs.camunda.io/docs/components/modeler/bpmn/message-events/"
    }
  ]
}
