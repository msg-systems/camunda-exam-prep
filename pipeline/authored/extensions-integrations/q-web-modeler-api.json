{
  "id": "ext-web-modeler-api",
  "topic": "extensions-integrations",
  "subtopic": "Web Modeler API",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Keystone Bank automates BPMN deployments for 5 microservices and 8000 events per day on Camunda 8.8. The team must trigger deployment of a BPMN file managed in Web Modeler to a target cluster via a CI/CD pipeline without copying the file to a local repository.",
  "question": "Which Web Modeler integration deploys a managed BPMN file from a CI/CD pipeline?",
  "options": [
    { "id": "a", "text": "Call the Web Modeler API endpoint to deploy the file by id to the target cluster; the API authenticates via OAuth client credentials" },
    { "id": "b", "text": "Schedule a Web Modeler webhook that fires on every save; the webhook deploys the file to the target cluster on every modeler save by every developer" },
    { "id": "c", "text": "Call the Zeebe gRPC endpoint with the file URL from Web Modeler; the broker fetches the file from Web Modeler at deploy time over the same gRPC channel" },
    { "id": "d", "text": "Call the Optimize API import endpoint with the file id; Optimize forwards the file to the target cluster's Zeebe broker as part of the same call" }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": { "text": "Correct. The Web Modeler API exposes a deploy endpoint that pushes a managed file by id to the target cluster, authenticated via OAuth client credentials." },
    "b": { "text": "Incorrect. Auto-deploying on every save is not the documented CI/CD pattern; the API endpoint is invoked explicitly from the pipeline." },
    "c": { "text": "Incorrect. The broker does not fetch files from Web Modeler; the Web Modeler API performs the deploy itself." },
    "d": { "text": "Incorrect. Optimize is an analytics component, not a deployment intermediary for BPMN files." }
  },
  "explanation": "The Web Modeler API exposes a deploy endpoint that pushes a managed file to the target cluster; CI/CD pipelines call it via OAuth client credentials.",
  "docs": [
    { "title": "Web Modeler API", "url": "https://docs.camunda.io/docs/apis-tools/web-modeler-api/" }
  ]
}
