{
  "id": "ext-properties-reference-61",
  "topic": "extensions-integrations",
  "subtopic": "deprecated-properties",
  "difficulty": "medium",
  "style": "scenario",
  "kind": "single",
  "camundaVersion": "8.8",
  "question": "An older config still sets `common.url=https://camunda.example.com`. Which Camunda 8.8 property is the documented replacement, given that the REST address is the unified endpoint for all interaction with Camunda?",
  "options": [
    {
      "id": "a",
      "text": "`camunda.client.rest-address`"
    },
    {
      "id": "b",
      "text": "`camunda.client.url`"
    },
    {
      "id": "c",
      "text": "`camunda.client.cloud.base-url`"
    },
    {
      "id": "d",
      "text": "`common.base-url`"
    }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": {
      "text": "The `common` deprecation table maps `common.url` to `camunda.client.rest-address` and explicitly notes that the REST address is the unified endpoint for all interaction with Camunda."
    },
    "b": {
      "text": "There is no `camunda.client.url` property in the reference; addressing flows through `rest-address` (and `grpc-address` for the gRPC channel)."
    },
    "c": {
      "text": "`cloud.base-url` is itself a deprecated SaaS-composition property, replaced by `cloud.domain`. It is not the replacement for `common.url`."
    },
    "d": {
      "text": "`common.base-url` is another deprecated alias in the same table; it also redirects to `camunda.client.rest-address`, not back into the `common.*` namespace."
    }
  },
  "explanation": "Camunda 8.8 funnels REST traffic for the orchestration cluster and its component APIs through a single REST address, `camunda.client.rest-address`. Both `common.url` and `common.base-url` from the legacy namespace are redirected onto it.",
  "docs": [
    {
      "title": "Camunda Spring Boot Starter - properties reference",
      "url": "https://docs.camunda.io/docs/apis-tools/camunda-spring-boot-starter/properties-reference/"
    }
  ]
}
