{
  "id": "ext-tasklist-api",
  "topic": "extensions-integrations",
  "subtopic": "Tasklist API",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Yarrow Insurance builds a custom Tasklist UI for 18 agents and 4000 user tasks per day in Camunda 8.8. The team needs to list the open user tasks assigned to the calling user, assign or claim a task, and complete a task with the form's variables payload via the platform's task-oriented API.",
  "question": "Which Camunda 8 API surface exposes user task list, claim, and complete operations?",
  "options": [
    { "id": "a", "text": "The Tasklist API exposes endpoints to search, assign, claim, and complete user tasks; the broker exposes only job-level operations in the Zeebe API" },
    { "id": "b", "text": "The Zeebe gRPC API exposes user task assign and complete operations directly; user tasks are a special job type and the Tasklist API is a thin proxy to it" },
    { "id": "c", "text": "The Operate API exposes user task list, claim, and complete operations; Tasklist is an aggregator of the same Operate API endpoints for human task workflows" },
    { "id": "d", "text": "The Identity API exposes user task list and claim operations; user task assignment is treated as a role grant rather than a process engine operation" }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": { "text": "Correct. The Tasklist API exposes the human-task surface (search, assign, claim, complete); Zeebe handles only the underlying job lifecycle." },
    "b": { "text": "Incorrect. Zeebe gRPC handles jobs, not user task-level assign/claim operations directly." },
    "c": { "text": "Incorrect. Operate is operational data and admin; user task list/claim/complete operations live in the Tasklist API." },
    "d": { "text": "Incorrect. Identity manages users, roles, and tokens, not user task lifecycle." }
  },
  "explanation": "The Tasklist API exposes user task search, assign, claim, and complete operations as the documented surface for human task UIs.",
  "docs": [
    { "title": "Tasklist API", "url": "https://docs.camunda.io/docs/apis-tools/tasklist-api-rest/" }
  ]
}
