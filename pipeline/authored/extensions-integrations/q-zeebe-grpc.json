{
  "id": "ext-zeebe-grpc",
  "topic": "extensions-integrations",
  "subtopic": "Zeebe gRPC",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Lumen Logistics consumes 30000 jobs per day in Camunda 8.8 from a custom job worker written in Go. The team uses the native Zeebe protocol directly for the lowest activation latency and must understand which transport the broker exposes for high-throughput worker subscriptions.",
  "question": "Which protocol does the Zeebe broker natively expose for job worker activation in Camunda 8?",
  "options": [
    { "id": "a", "text": "Zeebe exposes a gRPC API; clients open a streaming activation channel and receive jobs as messages on the channel for the lowest activation latency" },
    { "id": "b", "text": "Zeebe exposes a SOAP API; clients post activation requests as XML envelopes and receive job activations as XML responses on every poll cycle" },
    { "id": "c", "text": "Zeebe exposes a GraphQL API; clients open a subscription on the activation query and receive jobs as subscription events on the same channel for low latency" },
    { "id": "d", "text": "Zeebe exposes an AMQP API; clients subscribe to a per-type queue on the broker and receive jobs as AMQP messages with delivery acknowledgements per call" }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": { "text": "Correct. Zeebe natively exposes a gRPC API; job workers use it for activation streaming and command requests." },
    "b": { "text": "Incorrect. Zeebe does not expose a SOAP API; gRPC is the native protocol." },
    "c": { "text": "Incorrect. Zeebe does not expose a GraphQL API; gRPC is the native protocol." },
    "d": { "text": "Incorrect. Zeebe does not expose an AMQP API; gRPC is the native protocol." }
  },
  "explanation": "Zeebe natively exposes a gRPC API; job workers use it for low-latency activation streaming and command requests.",
  "docs": [
    { "title": "Zeebe gRPC API", "url": "https://docs.camunda.io/docs/apis-tools/zeebe-api/grpc/" }
  ]
}
