{
  "id": "ext-spring-jobworker-annotation",
  "topic": "extensions-integrations",
  "subtopic": "Spring SDK",
  "difficulty": "easy",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Beacon Insurance writes a Spring Boot job worker that processes 22000 jobs per day in Camunda 8.8 for the type credit-check. The team wants the framework to scan for worker methods on Spring beans and subscribe them to the broker without manual fluent calls in main().",
  "question": "Which Camunda 8 Spring SDK annotation declares a method as a job worker subscriber?",
  "options": [
    { "id": "a", "text": "Annotate the method with @JobWorker(type = \"credit-check\"); the Spring SDK auto-subscribes to the type at application start" },
    { "id": "b", "text": "Annotate the method with @ZeebeWorker(type = \"credit-check\"); the Spring SDK auto-subscribes at start and is the only auto-registration mechanism" },
    { "id": "c", "text": "Annotate the bean class with @WorkerType(\"credit-check\"); the Spring SDK auto-subscribes the bean's only public method at start" },
    { "id": "d", "text": "Annotate the method with @Subscribe(\"credit-check\"); the Spring SDK auto-subscribes the method and resolves variable bindings from the method parameters" }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": { "text": "Correct. The Camunda 8 Spring SDK uses @JobWorker with a type attribute on a bean method to register a subscriber at start time." },
    "b": { "text": "Incorrect. @ZeebeWorker is the legacy name; the current Spring SDK annotation is @JobWorker." },
    "c": { "text": "Incorrect. Subscription is per-method via @JobWorker, not at the class level via @WorkerType." },
    "d": { "text": "Incorrect. There is no @Subscribe annotation in the Camunda 8 Spring SDK; the documented annotation is @JobWorker." }
  },
  "explanation": "The Camunda 8 Spring SDK uses @JobWorker(type = ...) on bean methods; the framework auto-subscribes them at application start.",
  "docs": [
    { "title": "Camunda 8 Spring SDK", "url": "https://docs.camunda.io/docs/apis-tools/spring-zeebe-sdk/getting-started/" }
  ]
}
