{
  "id": "mgmt-cancel-process-instance",
  "topic": "managing-development",
  "subtopic": "Operate",
  "difficulty": "easy",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Sable Bank processes 11000 transactions per day in Camunda 8.8. A duplicate process instance was started accidentally on a customer record. The operator must terminate the duplicate from Operate so the in-flight activities are cancelled and downstream branches do not run after the action.",
  "question": "Which Operate action cancels a running process instance and stops every active token within it?",
  "options": [
    { "id": "a", "text": "Cancel the process instance from its detail page; the engine terminates every active token and marks the instance as cancelled" },
    { "id": "b", "text": "Delete the process definition from the deployments view; every running instance of that definition is then implicitly cancelled by the engine" },
    { "id": "c", "text": "Pause the process instance from its detail page; cancellation requires a follow-up admin API call against the paused instance afterwards" },
    { "id": "d", "text": "Restart the broker leader for the instance's partition; the engine recreates partition state without the duplicate process instance present" }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": { "text": "Correct. Operate exposes a Cancel action on the process instance detail page; the engine terminates every token and marks the instance as cancelled." },
    "b": { "text": "Incorrect. Deleting a deployment does not implicitly cancel running instances; running instances continue against their existing definition version." },
    "c": { "text": "Incorrect. There is no Pause action in Operate; cancellation is direct and does not require a separate admin API call." },
    "d": { "text": "Incorrect. Restarting the broker leader does not selectively cancel instances; it is a cluster-level action with no per-instance scope." }
  },
  "explanation": "Operate's Cancel action terminates every active token in the instance and marks it cancelled, which is the documented way to stop a single duplicate instance.",
  "docs": [
    { "title": "Operate cancel process instance", "url": "https://docs.camunda.io/docs/components/operate/userguide/cancel-process-instance/" }
  ]
}
