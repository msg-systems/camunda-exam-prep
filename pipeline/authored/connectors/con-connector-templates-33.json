{
  "id": "con-connector-templates-33",
  "topic": "connectors",
  "subtopic": "inbound-boundary",
  "difficulty": "medium",
  "style": "scenario",
  "kind": "single",
  "camundaVersion": "8.8",
  "question": "On an HTTP webhook boundary event template, the user leaves the `Webhook method` dropdown at its default value `any`. What does that configure for the webhook endpoint?",
  "options": [
    {
      "id": "a",
      "text": "The endpoint accepts any of the listed HTTP methods (`GET`, `POST`, `PUT`, `DELETE`) and triggers the connector for each."
    },
    {
      "id": "b",
      "text": "The endpoint only accepts `GET` requests until an explicit method is chosen."
    },
    {
      "id": "c",
      "text": "The runtime issues `any` available HTTP method in an outbound polling loop against the configured URL."
    },
    {
      "id": "d",
      "text": "The endpoint auto-selects the HTTP method based on the boundary event's payload Content-Type header."
    }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": {
      "text": "The template's `inbound.method` dropdown lists choices `Any`, `GET`, `POST`, `PUT`, `DELETE` with `any` as the default. Selecting `any` means the webhook endpoint accepts any of those HTTP methods and fires the connector each time."
    },
    "b": {
      "text": "The default value of the dropdown is `any`, not `get`; only when the user explicitly picks `GET` does the endpoint become GET-only."
    },
    "c": {
      "text": "Webhook inbound connectors are triggered by incoming HTTP requests — they do not poll an external URL; outbound polling is a separate connector subtype."
    },
    "d": {
      "text": "The template does not derive the HTTP method from a Content-Type header; the user picks it explicitly or leaves it as `any`."
    }
  },
  "explanation": "The webhook boundary event template's `inbound.method` property is a Dropdown with choices `Any`, `GET`, `POST`, `PUT`, `DELETE` and default value `any`. With `any` selected, the webhook endpoint accepts all four HTTP methods; choosing a specific method restricts the endpoint to that verb. Webhook inbound is request-driven — the runtime listens for incoming HTTP calls rather than polling.",
  "docs": [
    {
      "title": "Connector templates",
      "url": "https://docs.camunda.io/docs/components/connectors/custom-built-connectors/connector-templates/"
    }
  ]
}
