{
  "id": "dev-getting-started-orchestrate-human-tasks-01",
  "topic": "dev-environment",
  "subtopic": "forms-variable-binding",
  "difficulty": "easy",
  "style": "scenario",
  "kind": "single",
  "camundaVersion": "8.8",
  "question": "A user task is followed by an exclusive gateway whose conditions read the process variable `meal`. In the form, a `Radio` component has been added with the static options `Chicken` and `Salad`. Which property of the `Radio` component must be set to `meal` so the user's selection is exposed as that process variable?",
  "options": [
    {
      "id": "a",
      "text": "`name`"
    },
    {
      "id": "b",
      "text": "`key`"
    },
    {
      "id": "c",
      "text": "`label`"
    },
    {
      "id": "d",
      "text": "`id`"
    }
  ],
  "correctOptionId": "b",
  "optionExplanations": {
    "a": {
      "text": "The component's `name` is a descriptive title shown in the properties panel of the form editor; it does not bind the component to a process variable."
    },
    "b": {
      "text": "The `key` maps the component to a process variable. The Radio's selected value is stored under this key, so setting it to `meal` exposes the user's selection as the `meal` process variable consumed by the gateway."
    },
    "c": {
      "text": "`label` is the visible text shown next to each static option (for example `Chicken`); it controls how options are rendered, not which process variable receives the selection."
    },
    "d": {
      "text": "The Form ID identifies the form itself for binding to a user task; it is not the property that names the process variable holding the user's selection."
    }
  },
  "explanation": "In Camunda 8 forms, each input component has a `key` property that maps it to a process variable: the component's value is stored under that key and becomes available to downstream BPMN elements. In the Getting started guide, the Radio component's `key` is set to `meal` so the chosen option (`Chicken` or `Salad`) drives the exclusive gateway that follows the user task.",
  "docs": [
    {
      "title": "Getting started: Orchestrate human tasks",
      "url": "https://docs.camunda.io/docs/guides/getting-started-orchestrate-human-tasks/"
    }
  ]
}
