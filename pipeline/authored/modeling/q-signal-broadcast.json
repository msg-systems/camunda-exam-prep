{
  "id": "mod-signal-broadcast",
  "topic": "modeling",
  "subtopic": "events",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Meridian Bank monitors 9000 customers per day in Camunda 8.8. A market-down signal must be received by every currently-running trade process at the same time so each one can switch to a degraded mode, independent of any correlation key value.",
  "question": "Which BPMN event element implements this broadcast notification to every subscribed running instance?",
  "options": [
    { "id": "a", "text": "Signal events are broadcast to every active subscriber by signal name and do not use correlation key matching at all" },
    { "id": "b", "text": "Message events are broadcast to every active subscriber by message name and use the engine global namespace as the correlation key" },
    { "id": "c", "text": "Escalation events are broadcast to every active subscriber by escalation code across all running processes in the same tenant" },
    { "id": "d", "text": "Error events are broadcast to every active subscriber by error code regardless of which activity threw the original error" }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": { "text": "Correct. Signal events in BPMN broadcast to all subscribers by signal name and do not require a correlation key, unlike messages." },
    "b": { "text": "Incorrect. Messages are point-to-point and require a correlation key to find a specific running instance; they are not broadcast." },
    "c": { "text": "Incorrect. Escalation events bubble up through parent scopes inside a single process instance and are not broadcast across instances." },
    "d": { "text": "Incorrect. Error events are local to the throwing scope and matched by error code on a boundary or event subprocess; they are not broadcasts." }
  },
  "explanation": "Signal events are broadcast by signal name to every subscribed process; they are the documented Camunda 8 mechanism for fan-out notifications.",
  "docs": [
    { "title": "BPMN signal events", "url": "https://docs.camunda.io/docs/components/modeler/bpmn/signal-events/" }
  ]
}
