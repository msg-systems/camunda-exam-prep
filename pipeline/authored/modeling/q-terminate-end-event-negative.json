{
  "id": "mod-terminate-end-event-negative",
  "topic": "modeling",
  "subtopic": "end events",
  "difficulty": "medium",
  "style": "concept",
  "kind": "negative",
  "camundaVersion": "8.8",
  "question": "Which statement about a terminate end event is NOT true?",
  "options": [
    { "id": "a", "text": "It destroys all tokens within its containing scope." },
    { "id": "b", "text": "When placed inside an embedded subprocess, it only ends that subprocess; the parent process continues." },
    { "id": "c", "text": "When placed at the top level of the process, it ends the entire process instance." },
    { "id": "d", "text": "It automatically triggers compensation handlers for completed activities in its scope before ending." }
  ],
  "correctOptionId": "d",
  "optionExplanations": {
    "a": { "text": "True. A terminate end event ends every active execution inside its containing scope at once." },
    "b": { "text": "True. The terminate scope is the activity that contains it, so an embedded subprocess only ends itself; the parent flow continues." },
    "c": { "text": "True. At the top level, the containing scope is the whole process instance, so the instance ends." },
    "d": { "text": "False. Terminate ends its scope abruptly and does not trigger compensation. If compensation is needed, a separate compensation throw event must be placed in the flow." }
  },
  "explanation": "A terminate end event ends every active execution inside its containing scope at once. It does not invoke compensation handlers; compensation must be modelled explicitly with a compensation throw event.",
  "docs": [
    { "title": "BPMN terminate end event", "url": "https://docs.camunda.io/docs/components/modeler/bpmn/terminate-events/" }
  ]
}
