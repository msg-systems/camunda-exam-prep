{
  "id": "dec-decision-requirements-graph-51",
  "topic": "decisions-business-rules",
  "subtopic": "required decisions",
  "difficulty": "hard",
  "style": "concept",
  "kind": "single",
  "camundaVersion": "8.8",
  "question": "A dependent decision uses the result of required decision table `dish`, which has output names `entree` and `dessert`. How does the dependent decision access the `entree` value in FEEL?",
  "options": [
    {
      "id": "a",
      "text": "`dish.entree` — multi-output values are grouped under the decision ID and accessed by output name."
    },
    {
      "id": "b",
      "text": "`dish` — the engine collapses multi-output results to the first output's value."
    },
    {
      "id": "c",
      "text": "`entree` — output names of required decisions are top-level in the dependent decision's scope."
    },
    {
      "id": "d",
      "text": "`dish[0].entree` — multi-output decisions always produce a single-element list, regardless of hit policy."
    }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": {
      "text": "When a required decision is a decision table with more than one output, output values are grouped under the decision ID and accessed by their output names — for example, `dish.entree`."
    },
    "b": {
      "text": "The engine does not collapse multi-output tables to a single value; each output is accessed by name under the decision ID."
    },
    "c": {
      "text": "Output names are scoped under the required decision's ID, not exposed at the dependent decision's top level."
    },
    "d": {
      "text": "Whether the result is a list depends on the table's hit policy (e.g. `Collect`); it is not unconditionally `[result]`."
    }
  },
  "explanation": "For a required decision that is a decision table with multiple outputs, dependent decisions access each output as `decisionId.outputName`. The wrapping shape of the result (single value vs list) depends on the table's hit policy.",
  "docs": [
    {
      "title": "DMN decision requirements graph",
      "url": "https://docs.camunda.io/docs/components/modeler/dmn/decision-requirements-graph/"
    }
  ]
}
