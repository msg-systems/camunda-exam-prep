{
  "id": "dec-literal-vs-table",
  "topic": "decisions-business-rules",
  "subtopic": "decision logic",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Harbor Telco computes a single derived value for 25000 calls per day in Camunda 8.8: callCostEur = duration * ratePerSecondEur. The team does not want a 1-row decision table and prefers expressing the formula inline without input columns, output columns, or hit policy ceremony.",
  "question": "Which decision logic type fits a single FEEL expression that returns one value without input/output columns?",
  "options": [
    { "id": "a", "text": "A decision literal expression evaluates a single FEEL expression and returns one value as the decision's result" },
    { "id": "b", "text": "A decision table with one rule and the UNIQUE hit policy is the only supported way to express a single-formula decision" },
    { "id": "c", "text": "A business knowledge model defines a reusable function and replaces the decision element entirely in the DMN file" },
    { "id": "d", "text": "A knowledge source describes the authority and computes the FEEL expression at deployment time on behalf of the decision" }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": { "text": "Correct. The DMN modeler docs list decision literal expression as a supported decision logic type for evaluating a single FEEL expression and returning one value." },
    "b": { "text": "Incorrect. A single-rule table works but is unnecessary; the documented purpose of a literal expression is exactly this case." },
    "c": { "text": "Incorrect. A business knowledge model is invoked from a decision; it does not replace the decision element itself." },
    "d": { "text": "Incorrect. Knowledge sources have no execution semantics and are ignored during evaluation." }
  },
  "explanation": "Decision literal expression is the documented decision logic type for evaluating a single FEEL expression and returning one value, with no table ceremony.",
  "docs": [
    { "title": "Decision literal expression", "url": "https://docs.camunda.io/docs/components/modeler/dmn/decision-literal-expression/" }
  ]
}
