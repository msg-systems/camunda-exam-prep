{
  "id": "cp-feel-temporal",
  "topic": "configuring-processes",
  "subtopic": "FEEL",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Yarrow Bank processes 12000 transactions per day in Camunda 8.8. A FEEL output mapping must compute the next-business-day date by adding 1 day to today and writing the result as the dueDate process variable for downstream timer events to use.",
  "question": "Which FEEL expression adds 1 day to the current date in Camunda 8 FEEL?",
  "options": [
    { "id": "a", "text": "today() + duration(\"P1D\") uses the FEEL date function and ISO 8601 duration to add 1 day to the current date" },
    { "id": "b", "text": "today() + 1 uses FEEL number coercion to add 1 day to the current date and return a date value as the result" },
    { "id": "c", "text": "date.add(today(), 1) calls a built-in FEEL date helper that takes a date and a day count and returns a new date value" },
    { "id": "d", "text": "today() + P1D treats P1D as an unquoted FEEL identifier referring to a built-in 1-day constant for date arithmetic" }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": { "text": "Correct. FEEL adds durations to dates with the date(date) + duration(ISO8601) form; today() returns a date and duration(\"P1D\") is one day." },
    "b": { "text": "Incorrect. FEEL does not coerce numbers to durations; adding a bare 1 to a date is a type error." },
    "c": { "text": "Incorrect. FEEL has no date.add helper; the standard arithmetic operator handles the case via durations." },
    "d": { "text": "Incorrect. P1D is not a FEEL identifier; durations are created with the duration(string) function." }
  },
  "explanation": "FEEL date arithmetic uses date + duration(...) with ISO 8601 duration strings; today() + duration(\"P1D\") yields tomorrow's date.",
  "docs": [
    { "title": "FEEL temporal expressions", "url": "https://docs.camunda.io/docs/components/modeler/feel/language-guide/feel-temporal-expressions/" }
  ]
}
