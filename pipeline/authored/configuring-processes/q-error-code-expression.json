{
  "id": "cp-error-code-expression",
  "topic": "configuring-processes",
  "subtopic": "error events",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Junction Retail handles 7500 orders per day in Camunda 8.8. A FEEL expression on an end error event must compute the BPMN error code from a runtime variable (e.g., \"INVENTORY_\" + skuPrefix) so a matching boundary error event with the same dynamic code catches the error.",
  "question": "How is a dynamic FEEL-driven BPMN error code authored on the throw error end event?",
  "options": [
    { "id": "a", "text": "Set the error code expression on the end error event to a FEEL expression like =\"INVENTORY_\" + skuPrefix; the engine evaluates it at throw time" },
    { "id": "b", "text": "Set the error code on the end error event to the literal string INVENTORY_*; the engine substitutes the wildcard from the process variable" },
    { "id": "c", "text": "Set the error code on the end error event to a JUEL expression referencing skuPrefix; Camunda 8 supports JUEL expressions for error codes on the end error event" },
    { "id": "d", "text": "Set the error code on the end error event to %skuPrefix%; the engine performs environment-style variable expansion at throw time" }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": { "text": "Correct. Camunda 8 error events accept a FEEL error code expression that the engine evaluates at throw time to produce the runtime error code." },
    "b": { "text": "Incorrect. There is no wildcard substitution in BPMN error codes; the documented mechanism is a FEEL expression." },
    "c": { "text": "Incorrect. Camunda 8 uses FEEL, not JUEL, for runtime expressions on BPMN elements." },
    "d": { "text": "Incorrect. There is no percent-sign expansion in Camunda 8 error codes; the documented expression language is FEEL." }
  },
  "explanation": "BPMN error events in Camunda 8 accept a FEEL error code expression that is evaluated at throw time to produce the runtime error code for matching boundary handlers.",
  "docs": [
    { "title": "BPMN error events", "url": "https://docs.camunda.io/docs/components/modeler/bpmn/error-events/" }
  ]
}
