{
  "id": "cp-user-task-assignee-expression",
  "topic": "configuring-processes",
  "subtopic": "user task",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Pinnacle Bank handles 9500 transactions per day in Camunda 8.8. A user task must be assigned to the variable accountManager at activation rather than to a static user. The team must configure the user task so Tasklist shows the task to the dynamic assignee resolved at runtime.",
  "question": "How does the modeler assign a user task to a value resolved from a process variable at activation?",
  "options": [
    { "id": "a", "text": "Set the assignee property to a FEEL expression like =accountManager so the engine resolves the value at activation and writes it to the task" },
    { "id": "b", "text": "Set the assignee property to the literal accountManager so the engine treats the value as a variable reference for backward compatibility" },
    { "id": "c", "text": "Set the candidate users property to a FEEL expression like =accountManager so Tasklist treats candidate users as the actual assignee at activation" },
    { "id": "d", "text": "Set the assignee property to a JUEL expression referencing accountManager so Camunda 8 evaluates JUEL expressions to resolve the runtime user identifier" }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": { "text": "Correct. The assignee property on a user task accepts a FEEL expression in Camunda 8; the engine resolves it at activation to a user identifier." },
    "b": { "text": "Incorrect. The literal value is treated as a user name string and is not implicitly resolved against a variable." },
    "c": { "text": "Incorrect. Candidate users define visibility, not the actual assignee; the assignee field is the documented assignment control." },
    "d": { "text": "Incorrect. Camunda 8 uses FEEL, not JUEL, for runtime expressions on user task properties." }
  },
  "explanation": "User tasks accept a FEEL assignee expression in Camunda 8; the engine evaluates it at activation and writes the resolved user identifier to the task.",
  "docs": [
    { "title": "BPMN user task", "url": "https://docs.camunda.io/docs/components/modeler/bpmn/user-tasks/" }
  ]
}
