{
  "id": "cp-process-variable-initialization",
  "topic": "configuring-processes",
  "subtopic": "variables",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Sable Insurance starts 10000 transactions per day in Camunda 8.8 through the create-process-instance API. Each instance must initialise the process variables retryCount=0 and currencyCode=EUR at the start event without relying on any downstream input mapping or service task call.",
  "question": "Which Camunda 8 mechanism seeds variables on a new process instance at start time?",
  "options": [
    { "id": "a", "text": "Pass the variables in the create-process-instance API payload, or set an input mapping on the start event of the process to compute them" },
    { "id": "b", "text": "Declare initial variables in the BPMN file under a process-level documentation element so the engine reads them at every new instance start" },
    { "id": "c", "text": "Define the variables in application.properties on the broker so every new process instance receives them at start across all process definitions" },
    { "id": "d", "text": "Add a service task before the start event so the engine activates a worker that writes the initial values to the process scope before the actual start" }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": { "text": "Correct. New instances accept a variables payload on the create-process-instance API, and start events also support input mappings to initialise variables." },
    "b": { "text": "Incorrect. Documentation is human-readable metadata and is not consulted by the engine for variable initialisation." },
    "c": { "text": "Incorrect. Broker config does not seed process variables; initialisation is at the process or instance level, not the broker level." },
    "d": { "text": "Incorrect. There is no element before the start event in BPMN; the start event itself is the entry point of the process." }
  },
  "explanation": "Camunda 8 initialises variables at start either via the create-process-instance API payload or via input mappings on the start event of the process definition.",
  "docs": [
    { "title": "Process variables", "url": "https://docs.camunda.io/docs/components/concepts/variables/" }
  ]
}
