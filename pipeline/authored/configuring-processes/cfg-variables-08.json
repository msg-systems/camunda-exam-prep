{
  "id": "cfg-variables-08",
  "topic": "configuring-processes",
  "subtopic": "variables",
  "difficulty": "easy",
  "style": "concept",
  "kind": "single",
  "camundaVersion": "8.8",
  "question": "An input mapping with `source: =orderId` and `target: reference` is defined on a service task. In which scope is the new variable `reference` created when the mapping is applied?",
  "options": [
    {
      "id": "a",
      "text": "As a local variable in the scope of the service task."
    },
    {
      "id": "b",
      "text": "In the root scope of the process instance, so it is visible everywhere."
    },
    {
      "id": "c",
      "text": "In the parent subprocess scope, if any; otherwise in the root scope."
    },
    {
      "id": "d",
      "text": "As a job-only variable that exists in the worker payload but is not stored as a process variable."
    }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": {
      "text": "The docs state that when an input mapping is applied, it creates a new local variable in the scope where the mapping is defined. For a service task, that is the task's own scope."
    },
    "b": {
      "text": "Input mappings deliberately do not write to the root scope; they create local variables in the activity's own scope so they remain isolated."
    },
    "c": {
      "text": "The new variable is created in the scope where the mapping is defined, not in any parent scope."
    },
    "d": {
      "text": "The mapped value is a real local process variable, not a transient job-only payload field."
    }
  },
  "explanation": "Input mappings can be defined on service, script, business rule, user, send tasks, call activities, and subprocesses. When applied, they create new local variables in the scope where the mapping is defined, isolating them from the parent/root scope unless explicitly propagated.",
  "docs": [
    {
      "title": "Variables",
      "url": "https://docs.camunda.io/docs/components/concepts/variables/"
    }
  ]
}
