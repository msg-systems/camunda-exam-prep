{
  "id": "cp-ai-agent-ad-hoc",
  "topic": "configuring-processes",
  "subtopic": "AI agent",
  "difficulty": "hard",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Vanta Marketplace introduces an AI agent step into a customer-support process with 2500 events per day in Camunda 8.8. The agent must call a model-defined dynamic set of tools where each tool is one inner activity in a subprocess, and the model decides at runtime which tools to invoke and in which order.",
  "question": "Which Camunda 8.8 BPMN subprocess type backs this AI agent pattern with model-selected tool invocations?",
  "options": [
    { "id": "a", "text": "An ad-hoc subprocess holds the tools; the AI agent step uses the FEEL completion condition to end the loop once the model finishes" },
    { "id": "b", "text": "A call activity holds the tools; the AI agent step invokes one of the deployed tool processes per iteration through the model decision" },
    { "id": "c", "text": "An event subprocess holds the tools; the AI agent step triggers the event subprocess's start event once per model-selected tool invocation" },
    { "id": "d", "text": "An embedded subprocess holds the tools in a fixed sequence flow; the AI agent step skips the unselected tools through conditional sequence flows" }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": { "text": "Correct. The AI agent pattern in Camunda 8.8 uses an ad-hoc subprocess whose inner activities are tools the model can choose to run in any order, with a completion condition driving the loop end." },
    "b": { "text": "Incorrect. Call activities invoke a single deployed process per activation; they do not model a model-selected set of inner tools." },
    "c": { "text": "Incorrect. Event subprocesses are triggered by events inside a parent scope, not by a model's tool-selection loop." },
    "d": { "text": "Incorrect. Embedded subprocesses follow fixed sequence flows; they cannot model an unordered tool set chosen at runtime by a model." }
  },
  "explanation": "Camunda 8.8's AI agent pattern is built on the ad-hoc subprocess; inner activities are tools, the model selects them dynamically, and the completion condition drives the loop end.",
  "docs": [
    { "title": "AI agent ad-hoc subprocess", "url": "https://docs.camunda.io/docs/components/modeler/bpmn/ad-hoc-subprocesses/" }
  ]
}
