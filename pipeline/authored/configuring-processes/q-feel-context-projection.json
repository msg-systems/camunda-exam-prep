{
  "id": "cp-feel-context-projection",
  "topic": "configuring-processes",
  "subtopic": "FEEL",
  "difficulty": "medium",
  "style": "scenario",
  "camundaVersion": "8.8",
  "scenario": "Ridge Logistics handles 6000 shipments per day in Camunda 8.8. A FEEL output mapping on a service task must extract two specific fields (id and weightKg) from a complex JSON variable named shipment and write them as separate top-level process variables on completion.",
  "question": "Which FEEL expression on the output mapping yields a context (map) containing just those two fields?",
  "options": [
    { "id": "a", "text": "Use a FEEL projection like { id: shipment.id, weightKg: shipment.weightKg } as the source expression on the output mapping" },
    { "id": "b", "text": "Use a FEEL spread expression like { ...shipment[id, weightKg] } as the source expression on the output mapping for the service task" },
    { "id": "c", "text": "Use a FEEL list projection like shipment[id, weightKg] as the source expression on the output mapping; FEEL pick converts lists to contexts" },
    { "id": "d", "text": "Use a FEEL XPath expression like /shipment/[id|weightKg] as the source expression on the output mapping for the service task" }
  ],
  "correctOptionId": "a",
  "optionExplanations": {
    "a": { "text": "Correct. FEEL supports inline context construction with key:value pairs, which is the documented way to project two named fields from a larger object." },
    "b": { "text": "Incorrect. FEEL does not have a spread or rest operator; explicit key:value pairs are the documented projection mechanism." },
    "c": { "text": "Incorrect. Bracket syntax on a context is a single key access, not a multi-key projection; FEEL has no built-in pick on contexts." },
    "d": { "text": "Incorrect. FEEL does not use XPath; it uses a context and list syntax described in the FEEL language guide." }
  },
  "explanation": "FEEL supports inline context construction with key:value pairs; this is the documented way to project specific fields from a larger object into a result context.",
  "docs": [
    { "title": "FEEL contexts", "url": "https://docs.camunda.io/docs/components/modeler/feel/language-guide/feel-context-expressions/" }
  ]
}
